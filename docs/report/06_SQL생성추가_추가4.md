# testWoo Foundry — 잔여 결함 M-1 ~ M-3 수정 (SQL 방언 정합 + 스모크 검출력)

## 0. 전제
- 로그 #75(P0~P2) 및 N-1~N-6, S-1 수정은 검증 완료. 되돌리지 말 것.
- 특히 아래는 그대로 유지한다:
  - `_isSchemaError` 는 `XTK-170`/`XTK-171` 만 매칭
  - `getIfExists` 는 `res.@id` 로 존재 판정, 컬렉션 접근 금지
  - `_schemaXml` 은 `Schema.toDocument().documentElement.toXMLString()` 경로
  - `_hasProcessing` 상단의 "물리 컬럼명 추정 금지" 주석
- 본 작업은 N-4(limitSelect) 지시가 불완전했던 부분을 바로잡고, 스모크 검출력을 확정한다.

### 근거 문서
- MSSQL: ORDER BY 는 파생 테이블/서브쿼리에서 무효 (TOP/OFFSET/FOR XML 동반 시에만 허용)
  https://learn.microsoft.com/en-us/answers/questions/1061604/the-order-by-clause-is-invalid-in-views-inline-fun
- T-SQL SELECT 구문 순서: `SELECT [ALL|DISTINCT] [TOP n] select_list`
  https://learn.microsoft.com/en-us/sql/t-sql/queries/select-transact-sql
- Oracle row limiting (`ORDER BY ... FETCH FIRST n ROWS ONLY`)
  https://blogs.oracle.com/sql/how-to-select-the-top-n-rows-per-group-with-sql-in-oracle-database
- sqlSelect(format, query) 규약
  https://experienceleague.adobe.com/developer/campaign-api/api/f-sqlSelect.html

---

## M-1 (P1) limitSelect 방언 조립 오류 — MSSQL 구문 오류 + 이중 래핑 잔재

### 문제 1: DISTINCT / TOP 순서
T-SQL 문법은 `SELECT DISTINCT TOP n ...` 이다. `SELECT TOP n DISTINCT ...` 는 구문 오류.
현재 `limitSelect` 는 `head = "SELECT TOP n "` 뒤에 `selectList` 를 그대로 이어 붙이는데,
`_toolProbeValues` 가 `selectList` 에 `"DISTINCT col AS tw_val"` 를 섞어 전달하므로
MSSQL 환경에서 `probe_values` 가 전량 실패한다.
→ `selectList` 문자열에 DISTINCT 를 섞는 방식 자체가 방언 분기와 충돌한다.

### 문제 2: limit() 잔존으로 인한 파생 테이블 ORDER BY
`limit(sql, n)` 이 `limitSelect("*", "(" + sql + ") tw_lim", "", n)` 을 호출한다.
인자 `sql` 이 이미 `ORDER BY` 를 포함한 완성 SELECT 인 경우
MSSQL 에서 "The ORDER BY clause is invalid in views, inline functions, derived tables,
subqueries, and common table expressions..." 오류가 발생한다.
"래핑 지점을 하나로" 라는 N-4 취지와도 어긋난다.

### 수정 A — `dialect().limitSelect` 시그니처 변경
```js
// limitSelect(selectList, fromClause, whereSql, n, opts)
//   opts = { distinct: false, orderBy: "1" }
// selectList 에 DISTINCT 를 직접 넣지 말 것 (방언별 위치가 다름).
function limitSelect(selectList, fromClause, whereSql, n, opts) {
  var lim = Number(n) || DEFAULT_SAMPLE;
  var o = opts || {};
  var dis = o.distinct ? "DISTINCT " : "";
  var ob = (o.orderBy === null) ? "" : String(o.orderBy || "1");

  // T-SQL: SELECT [DISTINCT] [TOP n] select_list
  var head = isMssql
    ? ("SELECT " + dis + "TOP " + lim + " ")
    : ("SELECT " + dis);

  var s = head + selectList + " FROM " + fromClause;
  if (whereSql) s = s + " WHERE " + whereSql;
  if (ob) s = s + " ORDER BY " + ob;
  if (isOracle) s = s + " FETCH FIRST " + lim + " ROWS ONLY";
  else if (!isMssql) s = s + " LIMIT " + lim;
  return s;
}
```
- `orderBy: null` 을 명시하면 ORDER BY 를 생략한다(파생 테이블 래핑 시 사용).
- Oracle 은 `DISTINCT` + `ORDER BY 1`(select 리스트 서수 참조)이 유효하므로 그대로 둔다.

### 수정 B — `limit(sql, n)` 제거
- `dialect()` 반환에서 `limit` 를 **삭제**한다.
- 레포 전체에서 `.limit(` grep → 호출부를 전부 `limitSelect` 로 전환.
- 완성된 SELECT 에 상한만 씌워야 하는 지점(dedup L3 등)이 있으면
  `limitSelect("*", "(" + sql + ") tw_lim", "", n, {orderBy: null})` 로 대체한다.
  (파생 테이블 안의 ORDER BY 를 새로 만들지 않는다)
- 부득이 `limit` 를 남겨야 하면 내부에서 인자 SQL 말미의 `ORDER BY` 절을 제거한 뒤 감쌀 것.
  단 **전환(삭제)을 우선**한다.

### 수정 C — 호출부 정리
- `testWooProbe.js` `run()` 샘플:
  `dial.limitSelect(kc + " AS tw_key", "(" + inner + ") tw_sample", "", lim)`
  (opts 생략 → distinct=false, orderBy="1") — 현행 유지, 시그니처만 호환 확인.
- `testWooToolkit.js` `_toolProbeValues`:
  `selectList` 에서 `DISTINCT` 문자열을 **제거**하고
  `dial.limitSelect(col + " AS tw_val", table, col + " IS NOT NULL", limit, {distinct:true})`
  형태로 전환.
- `testWooDedup.js` 의 EXCEPT/MINUS 대칭차 쿼리에서 limit 래핑을 쓰는 지점이 있으면
  `{orderBy: null}` 로 호출(집합 연산 내부 ORDER BY 금지).

### 검증
- 현재 DBMS(PostgreSQL 가정)에서 `probe_values` 정상 동작 회귀 확인.
- `dialect()` 를 강제로 mssql / oracle 로 시뮬레이션해 생성 SQL 문자열을 `logInfo` 출력,
  아래 형태와 일치하는지 육안 확인 후 로그에 첨부:
  - MSSQL:  `SELECT DISTINCT TOP 50 col AS tw_val FROM tbl WHERE col IS NOT NULL ORDER BY 1`
  - Oracle: `SELECT DISTINCT col AS tw_val FROM tbl WHERE col IS NOT NULL ORDER BY 1 FETCH FIRST 50 ROWS ONLY`
  - PG:     `SELECT DISTINCT col AS tw_val FROM tbl WHERE col IS NOT NULL ORDER BY 1 LIMIT 50`
- 실행 없이 문자열만 검증하는 단위 함수를 `tools/` 에 두어도 좋다(선택).

---

## M-2 (P2) 스모크 6번 검출력 불확실 — getIfExists 경로 미보장

### 문제
`twStepQueueRoundTrip` 이 `testWoo.repo.getQueueStatus(qid)` 에 의존하는데,
이 함수가 내부적으로 `operation="select"` 를 쓴다면 N-1(getIfExists 파싱) 회귀를 **잡지 못한다**.
스모크의 존재 이유가 그 회귀 검출이므로 경로가 보장되어야 한다.

### 수정
1. `new_ver/js/testWooRepository.js` 의 `getQueueStatus` 가 어떤 operation 을 쓰는지 확인한다.
2. `getIfExists` 경로가 아니면, 스모크가 **실제 getIfExists 경로를 직접 타도록** 보강한다.
   - `testWoo.foundry` 에 점검 전용 진입점을 노출하거나
     (`_getQueue` 를 `peekQueue(id)` 로 public 화, 읽기 전용·부작용 없음),
   - 또는 스모크 안에서 동일한 `operation="getIfExists"` queryDef 를 직접 구성해
     `res.@id` 판정과 필드 파싱을 검증한다.
3. 어느 쪽이든 스모크 6번은 다음 3가지를 모두 확인해야 한다:
   - getIfExists 결과에서 `@id` 가 읽히는가 (무매치 아님)
   - 존재하지 않는 id(예: `-1`) 조회 시 **null 반환**, 예외 아님
   - 필드값(`status="queued"`)이 기록값과 일치하는가
4. 6번 스텝을 `6a.queue.write-read` / `6b.queue.miss` 로 분리해 결과를 각각 리포트한다.

### 주의
- 무매치 검증(`-1` 조회)은 부작용이 없으므로 더미 생성 전에 먼저 수행해도 된다.
- 더미 레코드 `finally` 삭제 로직은 현행 유지(실패 시 `logError` 로 수동 삭제 안내).

---

## M-3 (P2) 스모크 1번 — testWoo 전역 부재 시 로그 노이즈

### 문제
`testWoo` 자체가 undefined 이면 14개 모듈이 전부 missing 으로 나열되어
"loadLibrary 실패" 라는 진짜 원인이 묻힌다. 스모크는 장애 상황에서 읽는 산출물이므로
메시지 품질이 곧 복구 속도다.

### 수정
```js
function twStepGlobals() {
  if (typeof testWoo === "undefined" || !testWoo) {
    twFail("1.globals", "testWoo 전역 자체가 없음 — loadLibrary 실패. " +
      "JS 라이브러리 배포 여부와 woo: 네임스페이스를 먼저 확인할 것");
    return false;
  }
  var names = [...];
  // 이하 기존 루프 유지
}
```
- 개별 모듈 누락과 전역 부재를 **다른 메시지**로 구분한다.

---

## 검증 절차
1. `tools/checkRhinoSyntax.js` 전체 통과.
2. 스모크 전 스텝 PASS (6a/6b 분리 반영).
3. 존재하지 않는 큐 id 조회 → 예외 없이 null (M-2).
4. `probe_values` 정상 동작 (M-1 회귀).
5. mssql/oracle 시뮬레이션 SQL 문자열 3종 육안 확인 후 로그 첨부 (M-1).
6. 레포 전체 `.limit(` grep 결과 0건 (M-1 수정 B 완료 확인).
7. `foundry.enabled=false` 회귀 경로 정상.

## 로그
`docs/log/log.md` 에 신규 항목 추가. M-1/M-2/M-3 각각의 원인·수정·근거 URL 기록.
M-1 은 "N-4 지시가 방언 분기를 고려하지 못한 후속 교정"임을 명시할 것.

## 금지
- `selectList` 문자열에 `DISTINCT` 직접 삽입.
- 파생 테이블/서브쿼리/집합연산 내부에 `ORDER BY` 생성.
- `getIfExists` 결과에 `.length` / `[0]` / 컬렉션 접근.
- 예외를 빈 catch 로 삼키기.
- woo:* 커스텀 스키마 물리 컬럼명 추정 SQL.
- 기존 P0~P2, N-1~N-6, S-1 수정 되돌리기.

## 작업 순서
M-2(스모크 경로 확정) → M-3 → M-1 수정 A → M-1 수정 B/C → 스모크 재실행 → 로그
