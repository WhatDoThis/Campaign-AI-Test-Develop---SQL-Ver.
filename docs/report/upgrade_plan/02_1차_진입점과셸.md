# 1차 · 진입점과 셸 (for AI)

> **AGENT_MODE**: Chat 첨부 시 구현 Agent.  
> **단독 완결**. WF 확장 폼(`testWooExtendWorkflow.xml`)은 **존치**.  
> 데이터 API(폴더/WKF) 연결 금지 — 목록은 더미.

## 0. 실행 프롬프트

```
[#144 · 1차 진입점과셸]
첨부: docs/report/upgrade_plan/02_1차_진입점과셸.md
선행: 0차 PASS, docs/report/11_고도화_추적표.md, 00-acc-guardrails
고유: navtree <commands>+rights 필수. 레이아웃 골격만, 데이터 더미. WF 확장 폼 존치.
금지: flex/grid/vh/var/calc, fetch/Promise, 상태머신 실데이터.
종료: acc-id-tracer + acc-verifier.
```

## 1. 차수 목표

Tools 메뉴에서 Studio를 열고, 3분할 셸(정보바/좌 AI/우 목록) 골격을 IE-safe로 고정한다.  
실제 캠페인·WKF 데이터는 넣지 않는다.

## 2. 커버 ID

| ID | 원문 | 이 차수 구현 |
|---|---|---|
| UI-1-1 | Tools에서 AI Studio 열기 | `testWooAiNavtree.xml` `<commands>` + `rights` 속성 명시. Tools에서 동작 확인 |
| UI-1-2 | explorer로 최종 통일 (부분) | Tools→view 경로 1차 완성. WF 임베드 제거는 8차 |
| UI-1-3 | jssp 이미 연결 | 기존 `testWooAiStudio.jssp` 재사용, URL 유지 |
| UI-1-4 | 추가기능 필요 | UI-2 셸로 수용 |
| UI-2-1 | 왼쪽 AI 입력창 | 좌 컬럼 composer+answer |
| UI-2-2 | 답변 상단 70% | table-cell + **고정 px** (vh 금지). 비율은 시각적 근사 |
| UI-2-3 | 요청 하단 20% | 동일, composer 고정 px |
| UI-2-4 | 오른쪽 목록창 | 우 컬럼. 더미 3행 (`[더미] Campaign A` 등) |
| UI-2-7 | 제목 아래 정보창 | 높이 **48px** 고정 바. 더미 텍스트 `캠페인: (미선택)` |

## 3. 선행 / 후속

- 선행: 0차 READY
- 후속 차단 시: 2차 상태머신 불가

## 4. 어도비 근거

- Navtree / Tools commands: https://experienceleague.adobe.com/en/docs/campaign-classic/using/configuring-campaign-classic/navigation-hierarchy/configuration  
  원문: *"Global commands are accessible from the main Tools menu"* — `form` 또는 `view`/`viewType`, `rights` 가능.
- JSSP namespace URL `/woo/*.jssp` (프로젝트 관례)

## 5. 변경 파일

| 경로 | 구분 | 요지 |
|---|---|---|
| `new_ver/navtree/testWooAiNavtree.xml` | 수정 | command `rights`, label 정리, URL 치환 주석 유지 |
| `new_ver/jssp/testWooAiStudio.jssp` | 수정 | 정보바+3분할 셸 마크업/CSS(table) |
| `new_ver/jssp/testWooAiStudioJs.jssp` | 수정 | 더미 목록 렌더, 정보바 갱신 stub |
| `new_ver/html/testWooAiStudio.js` | 동기 | 동일 |
| `docs/log/log.md` | 수정 | 로그 |

**존치(수정 최소화)**: `new_ver/input_form/testWooExtendWorkflow.xml`

## 6. 구현 상세

### Step 1 — navtree Tools (무엇을/어디에/왜)

- **무엇을**: `<command name="testWooAiStudio" … rights="admin,testWooAiOperator">` 형태로 named right 게이트(환경에 맞는 right 이름 — 없으면 문서 TBD로 HUMAN에 요청). `viewType="view"` 유지.
- **어디에**: `testWooAiNavtree.xml` `<commands>`
- **왜**: UI-1-1 공식 경로

### Step 2 — 셸 마크업

- **무엇을**:  
  - `#twInfoBar` 48px  
  - `#twMainTable` 2열: 좌(답변 px + 요청 px), 우(`#twListPane`)  
- **어디에**: `testWooAiStudio.jssp`  
- **왜**: UI-2-1~4·7. embed/standalone 둘 다 동일 골격(embed에서 funnel 숨김 유지 가능)

### Step 3 — 더미 데이터

- **무엇을**: JS 배열 3건을 우측에 그림. 클릭 시 정보바에 문자열만 반영(서버 호출 없음).
- **왜**: 2차 연결 전 레이아웃 검증

### Step 4 — v= bump

- Studio `v=` 상수 +1. DoD에 숫자 기입.

## 7. 금지사항

- 실폴더 queryDef, WKF 생성, 상태머신 enum 도입(이름은 stub만)
- WF 확장 폼 삭제/비활성
- flex/grid/vh/CSS변수/calc
- nodeModel로 캠페인 폴더 추가(2차에서 명시적으로 금지 재확인)

## 8. DoD

- [ ] Console → **Tools** → `Test Woo AI Studio`(또는 동일 label) 메뉴가 보인다
- [ ] 메뉴 클릭 시 Explorer/뷰에 Studio가 열리고 제목이 보인다
- [ ] 제목 바로 아래 정보바(~48px)에 `캠페인: (미선택)`류 텍스트가 보인다
- [ ] 왼쪽 상단 답변 영역 / 하단 요청 영역이 구분되어 보인다
- [ ] 오른쪽에 더미 목록 ≥1행이 보인다
- [ ] URL에 새 `v=` 값이 보인다
- [ ] 기존 WF 임베드 경로가 여전히 열린다(회귀)

## 9. HUMAN_CONSOLE

1. `woo:testWooAiNav` Navigation hierarchy 재등록
2. `__CAMPAIGN_SERVER_URL__` 실서버 URL 치환 확인
3. Named right 없으면 생성 또는 command `rights`를 임시 완화(메모 필수)
4. Tools 메뉴 노출 확인 (권한 없는 유저로는 안 보여야 함 — rights 설정 시)
5. JSSP 재배포 + 캐시 클리어
6. Chat에 `1차 HUMAN: …` 회신

## 10. 테스트·디버깅

### Agent 테스트

- [ ] IE 금지 API rg = 0
- [ ] CSS 금지 토큰 rg = 0
- [ ] html ↔ jssp 동기

### HUMAN 테스트

- [ ] Tools 진입
- [ ] WF embed 진입
- [ ] 창 리사이즈 시 셸 붕괴 없음(스크롤 허용)

### 디버그

| 증상 | 원인 후보 | 조치 |
|---|---|---|
| Tools에 메뉴 없음 | navtree 미등록/rights | hierarchy 재등록, right 확인 |
| 열렸는데 404 | URL 치환 누락 | secrets의 서버 URL |
| 정보바 안 보임 | CSS/HTML 누락 | jssp 재배포·v= |
| embed만 깨짐 | 폼 캐시 | `_r` / form 재등록 |

## 11. 롤백

- navtree/JSSP를 0차 태그로 재배포
- git revert 1차 커밋

## 12. TBD

- Named right 정확한 운영 이름(환경별) — HUMAN이 확정하면 navtree에 반영

## 13. 종료 게이트

커버 ID 9개 모두 문서·구현 매핑. `/acc-id-tracer` PASS.
