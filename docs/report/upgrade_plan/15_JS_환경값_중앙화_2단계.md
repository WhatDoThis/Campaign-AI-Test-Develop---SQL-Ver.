# 15 · JS·Studio STG 리팩터링 (환경값 + 코드정리)

> **상태:** `[ ]` 미착수 (2026-09-01 검수·계획 확정)  
> **전제:** STG schema/input_form/navtree 정리 PASS (#391) · ACC JS orphan 정리 PASS (4-A) · Tools-only Studio  
> **범위:** ① Env/Config 중앙화 · ② dead code·retire 잔재 제거 · ③ Tools-only Studio 단순화(Phase 3)  
> **정본:** `testWooEnv.js` = Git 계약·엔진 튜닝 · `testWooConfig.js` = Option 병합 · ACC Option = 템플릿 id·LLM **값 정본**  
> **관련:** `docs/main/(공용)STG_패키징_정리.md` §3-C · `docs/main/(공용)new_ver_ACC배포분류.md` §6

---

## 0. 한 줄 요약

| | |
|---|---|
| **문제 A** | `sCustomer_id`·템플릿 id·Foundry few-shot 등 **환경값 분산** |
| **문제 B** | WF embed retire 후 **dead code·repo orphan·Studio embed 분기** 잔존 |
| **해결** | Phase 1 Env 중앙화 → Phase 2 상수+dead code → Phase 3 Tools-only JSSP |
| **범위外** | `FragContract`·`Llm` **로직/분할** — mart 하드코딩 없음 · verified 엔진 유지 |

**repo JS:** 21파일 · ~17,985줄 — **전량 리팩터링 불필요**.  
**예상 diff:** ~600(Ph1) + ~350(Ph2) + ~400(Ph3 JSSP) = **~1,350줄**.

---

## 1. 선행 조건 (PASS 확인)

| # | 항목 | 확인 |
|---|---|---|
| P1 | ACC `memo.js` · `testWooWorkflowIo` · `testWooCodemap` orphan 삭제 | `[x]` 4-A 완료 |
| P2 | `testWooAiWorkflowUi` schema · `testWooExtendWorkflow` form ACC Delete | `[x]` |
| P3 | schema/input_form retire (Golden 등) | `[x]` #391 |
| P3b | **repo** `new_ver/schema/testWooAiWorkflowUi.xml` active tree 제거 | `[ ]` Phase 2 |
| P4 | JS 21개 ACC 등록 · `libVersions` allMatch | `[ ]` Phase 1 후 재확인 |
| P5 | Studio v=202 · Generate/Register smoke | `[ ]` Phase 1 HUMAN |

---

## 2. 분산 현황 (리팩터링 전)

| 값 | 현재 위치 | Phase |
|---|---|---|
| `grainKeyCandidates: ["sCustomer_id"]` | `testWooToolkit.js` env() | **1** |
| `populationCountSql: ""` (비어 있음) | `testWooEnv.js` | **1** — PoC 값 채움 |
| Foundry few-shot (`testWooSampleCustomer`, `iAge`, `sCustomer_id`) | `testWooFoundry.js` | **1** |
| Campaign/WKF 템플릿 id·이름·retired guard | `testWooWorkflowClone.js` | **1** |
| `aiStudioSql` / `customActivity` | `testWooWorkflowUi.js` | **1** |
| XtkOption LLM 3종 | `testWooConfig.js` | ✅ 유지 |
| `testWooAiAutoApprove` | `Foundry.js` 직접 getOption | **1** → cfg |
| `testWooAiInjectBackup` | `WorkflowUi.js` 직접 getOption | **1** → cfg |
| Match Jaccard 0.9 · limit 500 · top 10 | `testWooMatch.js` | **2** |
| Probe DEFAULT_SAMPLE 20 | `testWooProbe.js` | **2** |
| Studio list DEFAULT_LIMIT 200 | `testWooStudioContext.js` | **2** |
| `TW_TITLE_MAX` 200 | `testWooCommon.js` | **2** |
| `_hintCat` 축 taxonomy | `testWooEnPivot.js` | **유지** (mart 아님) |
| WorkflowUi SOAP Shell* (~150줄) | `testWooWorkflowUi.js` | **2** (필수) |
| BindPick Option helper | `WorkflowUi` `_twWfBindPick*` | **2** (SOAP와 동시) |
| repo orphan schema | `new_ver/schema/testWooAiWorkflowUi.xml` | **2** → `_retire/`만 |
| Studio embed CSS·분기 | `testWooAiStudio.jssp` · `StudioJs.jssp` | **3** |
| Toolkit `resetBudget` deprecated | `testWooToolkit.js` | **2** (호출 0건) |

**getOption 직접 호출 (JS):** `Config` · `Foundry` · `WorkflowClone` · `WorkflowUi` — Phase 1에서 **Config 경유로 통일**.

---

## 2-b. 환경값 외 리팩터링 후보 (JS 검수 2026-09-01)

> **있음.** Phase 1은 Env만 · Phase 2·3이 **코드 정리** 본체.

### R-A. dead code · retire 잔재 — **STG 권장 (Phase 2)**

| ID | 대상 | 줄·규모 | 이유 | Tools Register 영향 |
|---|---|---:|---|---|
| R-A1 | `WorkflowUi` SOAP `ShellProbe/Pick/Bind` | ~150 | embed schema·form ACC 삭제됨 · **호출 불가** | **없음** — `commitInject`만 사용 |
| R-A2 | `_twWfBindPickKey` · `_twWfReadBindPick` | ~25 | SOAP Pick/Bind 전용 | 없음 |
| R-A3 | `new_ver/schema/testWooAiWorkflowUi.xml` | 1파일 | `_retire/`에 정본 있음 · active tree orphan | repo 정리 |
| R-A4 | `Toolkit.resetBudget` export | ~5 | deprecated · **repo 호출 0건** | 없음 |
| R-A5 | `WorkflowUi` 헤더·Dependencies | 주석 | `ExtendWorkflow`·SOAP 설명 obsolete | 없음 |

**유지 (dead 아님):** `customActivity`→`aiStudioSql` normalize · `script`/`userScript` legacy read — **구 템플릿 WKF 마이그레이션용**.

### R-B. Tools-only Studio 단순화 — **Phase 3 (JSSP)**

| ID | 대상 | 규모 | 내용 |
|---|---|---:|---|
| R-B1 | `testWooAiStudio.jssp` | ~80줄 CSS + 분기 | `body.embed` · `?embed=1` · ExtendWorkflow 헤더 주석 |
| R-B2 | `testWooAiStudioJs.jssp` | ~30+ 분기 | `EMBED` · `SHELL_MODE` · embed 전용 hint/copy |
| R-B3 | `html/testWooAiStudio.js` | 동기 | StudioJs와 동기 유지 규칙 — **같이 수정** |
| R-B4 | boot litmus 문자열 | 소량 | `embed=0` 단일 경로 · v= bump |

**선행:** R-A1 PASS · Tools Studio smoke. **금지:** embed URL 경로 복구.

### R-C. 구조·일관성 — **Phase 2 (Env와 함께)**

| ID | 대상 | 작업 |
|---|---|---|
| R-C1 | `getOption` 분산 | Config 단일 게이트 (§2 표) |
| R-C2 | Probe `DEFAULT_SAMPLE` vs `env.probe.sampleLimit` | 중복 제거 |
| R-C3 | Match·Common·StudioContext 상수 | Env `match` · `ui` 섹션 |
| R-C4 | `testWoo.dbg` | libVersions 미포함 — **문서화만** (선택 export) |

### R-D. **하지 않음** (리팩터링 후보 아님)

| 대상 | 이유 |
|---|---|
| `FragContract.js` (3485줄) | verified 엔진 · mart 하드코딩 없음 · **분할 금지** |
| `Llm.js` (2635줄) | Pass0/1 파이프라인 · 프롬프트 예시는 LLM 지시문 |
| `EnPivot._hintCat` | 축 taxonomy 힌트 · domain enum 아님 |
| `Common.logon(deprecated)` | ACC 구 API 폴백 · 제거 시 구 JSSP 깨질 수 있음 |
| Foundry `foundry.dailyBudget` | Env 선언만 · 검사 미구현 — **로직 추가는 DEFERRED** |
| Match 알고리즘 (#154 value-aware) | `13` HUMAN 별도 · 본 Phase 범위外 |

### Phase ↔ 후보 매핑

| Phase | 초점 | 후보 ID |
|---|---|---|
| **1** | Env `[A][B][C]` · Config · mart/nms 소비자 | (환경값) |
| **2** | 상수 Env화 + **dead code + repo orphan** | R-A1~A5 · R-C1~C3 |
| **3** | Tools-only Studio JSSP | R-B1~B4 |

---

## 3. 목표 구조 — `testWooEnv.js` 3계층

```text
[A] options.keys  XtkOption 키 이름만 (Git · 값은 ACC 콘솔)
[B] nms.contract  internalName·aiActivityName (Git 정본 · 환경 공통)
[B] nms.fallback  *TemplateId·wkfMax (Option 미설정 ACC · lgu-test 예시)
[C] mart          PoC↔운영 교체 (grain · population COUNT · Foundry 예시)
[D] bootstrap     Config·WorkflowClone: Option → contract 검증 → lookup → fallback
[E] 기존          security · debug · guard · llm · foundry · triage · dedup · toolkit · probe
```

상세 Option 표·litmus: **`docs/report/16_ACC_Option_설정가이드.md`**

### 3-A. `[A] options` — 콘솔에서 수기 변경

| Option 키 | 용도 | 현재 소비자 |
|---|---|---|
| `testWooAiLlmApiKey` | LLM API 키 | Config → llm |
| `testWooAiLlmModel` | LLM 모델 | Config → llm |
| `testWooAiLlmEndpoint` | LLM URL | Config → llm |
| `testWooAiCampaignTemplateId` | Campaign 템플릿 id | WorkflowClone |
| `testWooAiWkfTemplateId` | WKF 템플릿 id | WorkflowClone |
| `testWooAiWkfTemplateName` | WKF 템플릿 internalName | WorkflowClone |
| `testWooAiWkfMaxPerCampaign` | 캠페인당 WKF 상한 | WorkflowClone |
| `testWooAiAutoApprove` | Foundry 자동 active | Foundry |
| `testWooAiInjectBackup` | Register inject 스냅샷 | WorkflowUi |
| `testWooAiMatchEnabled` | Match UI (기본 OFF) | JSSP (Phase 2 문서화) |

### 3-B. `[B] nms` — contract + fallback (Git)

| 키 | 역할 | Test fallback 예 |
|---|---|---|
| `campaignTemplateName` | **contract** internalName | `OPEmptyTemplate_AI` |
| `wkfTemplateName` | **contract** internalName | `wfEmptyTemplate_AI` |
| `aiActivityName` | **contract** Register 대상 | `aiStudioSql` |
| `campaignTemplateId` | fallback id (Option 없을 때) | Explorer `@id` 확인 후 기입 |
| `wkfTemplateId` | fallback id (Option 없을 때) | Explorer `@id` 확인 후 기입 |
| `wkfMaxPerCampaign` | fallback 상한 | `15` |
| `wkfMaxHardCap` | clamp 상한 | `20` |

**운영:** 템플릿 id 변경 = ACC Option만. Env id는 dev/Option 비운 환경용.

### 3-C. `[C] mart` — PoC 샘플 mart (운영 전환 시 **이 블록만** 교체)

| 키 | Test 서버 기본 | 소비자 |
|---|---|---|
| `grainKeyCandidates` | `["sCustomer_id"]` | Toolkit.env → Triage/Foundry 프롬프트 |
| `populationCountSql` | `SELECT COUNT(*) FROM testWooSampleCustomer` | Gates G-C · Dedup near |
| `foundryExampleSchema` | `woo:testWooSampleCustomer` | Foundry FRAGMENT_SCHEMA_EXAMPLE 조립 |
| `sampleTablePrefix` | `testWooSample` | Foundry `_sampleShortName` (선택) |

### 3-D. `testWooConfig.getConfig()` 확장 shape

```javascript
{
  llm, security, search, foundry, triage, toolkit, guard, debug,  // 기존
  nms: { campaignTemplateName, campaignTemplateId, wkfTemplateName, ... },
  mart: { grainKeyCandidates, populationCountSql, foundryExampleSchema, ... },
  options: { autoApprove, injectBackup, matchEnabled, ... }  // getOption 병합값
}
```

---

## 4. Phase 1 — 환경값 중앙화 (핵심)

**목표:** mart · nms · Option을 Env/Config로 모으고 소비자 5모듈 수정.  
**예상 diff:** ~450–600줄 · **7파일**.

| 파일 | 줄 | 작업 |
|---|---:|---|
| `new_ver/js/testWooEnv.js` | 232 | §3 `[A][B][C]` 섹션 추가 · 주석 구분 · `populationCountSql` PoC 기본값 |
| `new_ver/js/testWooConfig.js` | 101 | `getConfig()` → `nms` · `mart` · `options` · Option 읽기 통합 |
| `new_ver/js/testWooToolkit.js` | 1597 | `env()` → `cfg.mart.grainKeyCandidates` |
| `new_ver/js/testWooFoundry.js` | 2698 | few-shot을 `cfg.mart` 조립 · `isAutoApprove` → cfg |
| `new_ver/js/testWooWorkflowClone.js` | 760 | 템플릿 상수 → `cfg.nms` · `getOption` 제거 |
| `new_ver/js/testWooWorkflowUi.js` | 872 | activity명 · backup Option → cfg |
| `new_ver/jssp/testWooAiStudioContext.jssp` | — | `expectedByMod` env/cfg `__v` bump |

**변경 없음 (확인만):** `testWooGates.js` · `testWooDedup.js` — 이미 `cfg.foundry.populationCountSql` 경유.

### Phase 1 — 구현 체크리스트

| # | 항목 | Pass |
|---|---|---|
| 1.1 | `Env` 상단에 `[A][B][C]` 주석 블록 · 기존 `[E]` 섹션 유지 | `[ ]` |
| 1.2 | `mart.grainKeyCandidates` 단일 정본 · Toolkit 하드코딩 제거 | `[ ]` |
| 1.3 | `mart.populationCountSql` Test PoC 값 설정 | `[ ]` |
| 1.4 | Foundry `FRAGMENT_SCHEMA_EXAMPLE`이 `cfg.mart`에서 조립 | `[ ]` |
| 1.5 | WorkflowClone `getOption` 0건 · `cfg.nms` + bootstrap | `[ ]` |
| 1.6 | WorkflowUi activity명 · injectBackup → cfg | `[ ]` |
| 1.7 | Foundry `isAutoApprove` → cfg.options | `[ ]` |
| 1.8 | 모듈 헤더 docstring 갱신 (변경 파일) | `[ ]` |
| 1.9 | `testWoo.env.__v` / `testWoo.cfg.__v` bump · libVersions 동기 | `[ ]` |
| 1.10 | Rhino 구문 · `tools/checkRhinoSyntax.js` (로컬) | `[ ]` |

### Phase 1 — HUMAN litmus

| # | 시나리오 | 기대 |
|---|---|---|
| H1 | `libVersions` | `allMatch:true` · env/cfg mismatch 없음 |
| H2 | Generate (샘플 NL 1건) | grainKey plan에 `@key_column` 계열 · gate PASS |
| H3 | Register → WKF | `aiStudioSql` 주입 · template id Option/fallback 정상 |
| H4 | Foundry 큐 1건 (enabled 시) | few-shot schema가 `mart.foundryExampleSchema`와 일치 |
| H5 | G-C gate (population 설정 시) | `populationCountSql` 실행 · 95% 상한 동작 |

### Phase 1 — 배포 묶음

```
testWooEnv.js → testWooConfig.js → (Toolkit · Foundry · WorkflowClone · WorkflowUi)
→ testWooAiStudioContext.jssp (libVersions only)
```

ACC: JS Codes 재등록(순서 §3-C STG doc) → litmus → smoke.

---

## 5. Phase 2 — 상수 Env화 · dead code · repo 정리

**목표:** 알고리즘/UI 상수 Env 편입 · **embed dead code 제거** · repo orphan schema.  
**예상 diff:** ~300–350줄 · **8파일**.

| 파일 | 줄 | 작업 |
|---|---:|---|
| `new_ver/js/testWooMatch.js` | 708 | `env.match` — JACCARD_THRESHOLD · MATCH_SQL_LIMIT · DISCOVER_TOP_N |
| `new_ver/js/testWooProbe.js` | 213 | DEFAULT_SAMPLE → `cfg.probe.sampleLimit` (Env 중복 제거) |
| `new_ver/js/testWooStudioContext.js` | 74 | list limit → `env.ui.listLimit` |
| `new_ver/js/testWooCommon.js` | 354 | `TW_TITLE_MAX` → env |
| `new_ver/js/testWooEnv.js` | — | `match` · `ui` 섹션 추가 |
| `new_ver/js/testWooConfig.js` | — | 패스스루 |
| `new_ver/js/testWooWorkflowUi.js` | 872 | **R-A1** SOAP Shell* TRIM · **R-A2** BindPick · **R-A5** 헤더 |
| `new_ver/js/testWooToolkit.js` | — | **R-A4** `resetBudget` export 제거 |
| `new_ver/schema/testWooAiWorkflowUi.xml` | — | **R-A3** Delete → `_retire/wf_canvas_embed/`만 유지 |

**Phase 2 대상外 (로직 전용 · 12,400줄):**

`FragContract` · `Llm` · `Fragments` · `Compiler` · `Gates` · `Repository` · `Lifecycle` · `Dedup` · `Embedding` · `EnPivot` · `Feasibility`(toolkit.env 경로만 확인)

### Phase 2 — 구현 체크리스트

| # | 항목 | Pass |
|---|---|---|
| 2.1 | Match 임계값 Env `match` 섹션 단일 정본 | `[ ]` |
| 2.2 | Probe sampleLimit Env·Probe 중복 제거 | `[ ]` |
| 2.3 | StudioContext list limit Env 편입 | `[ ]` |
| 2.4 | Common TW_TITLE_MAX Env 편입 | `[ ]` |
| 2.5 | WorkflowUi SOAP·BindPick **완전 제거** · commitInject 유지 | `[ ]` |
| 2.6 | Toolkit resetBudget export 제거 | `[ ]` |
| 2.7 | repo `schema/testWooAiWorkflowUi.xml` active 제거 | `[ ]` |
| 2.8 | `__v` bump · libVersions (workflowUi) | `[ ]` |
| 2.9 | Match Option OFF 상태 회귀 (기본) | `[ ]` |
| 2.10 | Register → WKF inject smoke (SOAP 없이) | `[ ]` |

### Phase 2 — HUMAN litmus

| # | 시나리오 | 기대 |
|---|---|---|
| H6 | Match Option OFF | Studio 유사 목록 차단 (기존과 동일) |
| H7 | Validate probe | sampleLimit Env 값 반영 |
| H8 | listAiFolders limit | Env ui limit 반영 |
| H9 | Register (Phase 2 후) | SOAP 없이 commitInject 정상 |

---

## 5-b. Phase 3 — Tools-only Studio (JSSP)

**목표:** embed 미사용 전제 · Studio 단일 레이아웃 · §6 `(공용)new_ver_ACC배포분류` R-B.  
**예상 diff:** ~350–450줄 · **3파일** (+ v= bump).

| 파일 | 작업 |
|---|---|
| `new_ver/jssp/testWooAiStudio.jssp` | `embed` 파라미터·CSS·ExtendWorkflow 주석 제거 · Tools 단일 body |
| `new_ver/jssp/testWooAiStudioJs.jssp` | `EMBED`/`SHELL_MODE` 분기 축소 · hint/copy 단순화 |
| `new_ver/html/testWooAiStudio.js` | StudioJs와 동기 |

### Phase 3 — 체크리스트

| # | 항목 | Pass |
|---|---|---|
| 3.1 | Tools → Studio URL에 `embed=1` 없음 · 정상 로드 | `[ ]` |
| 3.2 | Program/Campaign/WKF 셸 · recentNl · Generate/Register 회귀 | `[ ]` |
| 3.3 | Studio `v=` bump · boot litmus `embed=0` | `[ ]` |
| 3.4 | `_retire/wf_canvas_embed/` 참조만 유지 (코드 복구 없음) | `[ ]` |

**선행:** Phase 2 H9 PASS.

---

## 6. 검수용 통합 체크리스트 (Phase 1~3 완료 후)

### 6-A. 구조

| # | 확인 | Pass |
|---|---|---|
| S1 | JS `getOption` 직접 호출 = **Config.js 내부만** | `[ ]` |
| S2 | `sCustomer_id` / sample mart 문자열 = **Env `[C] mart`만** | `[ ]` |
| S3 | 템플릿 id/이름 fallback = **Env `[B] nms`만** | `[ ]` |
| S4 | Option 키 이름 = **Env `[A] options` 레지스트리** | `[ ]` |
| S5 | `FragContract`·`Llm` diff 없음 (또는 env 무관) | `[ ]` |
| S6 | `ShellProbe/Pick/Bind` grep **0건** (JS) | `[ ]` |
| S7 | Studio `EMBED` 분기 **제거 또는 dead** (Phase 3) | `[ ]` |
| S8 | repo active tree에 `testWooAiWorkflowUi.xml` **없음** | `[ ]` |

### 6-B. downstream

| Stage | 모듈 | Pass |
|---|---|---|
| Generate | Toolkit.env · Foundry · Gates population | `[ ]` |
| Register | WorkflowClone · WorkflowUi | `[ ]` |
| Foundry WF | Foundry few-shot · autoApprove | `[ ]` |
| Studio UI | libVersions · listAiFolders | `[ ]` |
| Match JSSP | Option OFF 기본 | `[ ]` |

### 6-C. ACC 배포

| # | 항목 | Pass |
|---|---|---|
| D1 | JS 21개 재등록 (Env 선행) | `[ ]` |
| D2 | `libVersions` allMatch | `[ ]` |
| D3 | Studio `v=` litmus | `[ ]` |
| D4 | Phase 1: schema 변경 없음 · Phase 2: schema orphan delete만 | `[ ]` |
| D5 | Phase 3: JSSP v= litmus | `[ ]` |

### 6-D. 운영 전환 메모 (STG / 실 mart)

PoC → 운영 mart 전환 시 **`Env [C] mart` 블록만** 아래처럼 교체:

```javascript
mart: {
  grainKeyCandidates: ["<운영 grain sqlColumn>"],
  populationCountSql: "SELECT COUNT(*) FROM <운영 고객 mart>",
  foundryExampleSchema: "<namespace>:<schema>"
}
```

`foundry.namespaces` · `[E] foundry.enabled`는 별도 정책.

---

## 7. 금지 · 주의

| ID | 내용 |
|---|---|
| G1 | `FragContract`·`Compiler`·`Gates` 로직 변경 — env wiring만 |
| G2 | 도메인 if/switch로 컬럼명 분기 추가 (no-domain-hardcoding) |
| G3 | Match Option ON — Phase 2와 무관 · `13` HUMAN 별도 |
| G4 | `testWooAiMatchEnabled` AI/코드가 ON — 사용자 승인 전 금지 |
| G5 | ES6 · Promise · domain hardcoding in new_ver/js |

---

## 8. Agent 사용법

```
차수: JS-STG-15
가이드: upgrade_plan/15_JS_환경값_중앙화_2단계.md §4 | §5 | §5-b
선행: P1~P3 PASS · Phase N-1 HUMAN PASS
모드: Phase 1 (Env) | Phase 2 (Env+dead) | Phase 3 (JSSP Tools-only)
금지: G1~G5 · FragContract/Llm 로직·분할
```

**한 Chat = Phase 하나.** 권장 순서: **1 → 2 → 3**.

---

## 9. 관련 문서

| 문서 | 내용 |
|---|---|
| `docs/main/(공용)STG_패키징_정리.md` §3-C | JS 21개 · load 순서 |
| `docs/main/(공용)new_ver_ACC배포분류.md` §6 | WorkflowUi SOAP trim 예정 |
| `docs/report/12_STG_스키마_정리.md` | schema retire 정본 |
| `.cursor/rules/no-domain-hardcoding.mdc` | mart 값 Env 경유 원칙 |
| `14_미구현_DEFERRED_로드맵.md` | #178 · Codemap — 본 Phase와 무관 |

---

*작성: 2026-09-01 · log #394/#395 · STG JS 검수 · §2-b 코드정리 후보 추가*
