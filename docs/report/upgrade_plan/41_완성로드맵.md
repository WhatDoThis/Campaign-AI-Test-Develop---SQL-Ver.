# 완성 로드맵 — 다음 AI 정본 (2026-08-21)

> **이 파일 = 남은 일의 유일한 실행 순서.** 현황 숫자는 `01_진행판.md`.  
> 한 Chat = **파도 1개**(또는 파도 내 HUMAN 묶음). HUMAN을 AI가 PASS 처리하지 않음.  
> 구 가이드(02~15, 08-11 캠페인-먼저 UI)는 **읽되 신규 구현 금지.**  
> **진행 방식:** 중간 HUMAN은 **파도 끝 1~2회**만. §1-b Phase Gate로 중간 아이디어가 앞 설계를 침해하지 않게 한다.

제품 한 줄: 마케터가 한국어 조건 → **검증된 fragment SQL** → 캠페인/WKF에 넣고 Explorer에서 연다. LLM은 SQL을 쓰지 않는다.

---

## 0. 끝났다는 뜻 (ST6)

```
ST0 조건(+칩) → SQL     트랙 A  [#176 남음]
ST1~ST2 목록·확정       트랙 B  [repo 있음 · HUMAN]
ST3~ST5 Program/Campaign/WKF
ST6 주입 + [WKF 열기]    트랙 B  [R6 repo · HUMAN]
```

추출 키 `#164~#174 [x]` 는 ST0의 엔진만 닫은 것. **제품 완료 ≠ 추출 완료.**

---

## 1. 지금 시스템 (이미 있음 · 다시 만들지 말 것)

| 축 | 상태 | 정본 |
|---|---|---|
| NL→EN 슬롯→매칭→SQL | repo. 다축 분할·N월 YYYY-MM·leftover·debug | `37` `fragContract` `llm` |
| 상위어 IN (#175) | G1~G6 PASS. 5단계 UI repo v=182 | `38` · **HUMAN만** · G3 금지 |
| SQL-First 셸 ST0~ST6 | R3~R6 **repo**. 주입·열기는 HUMAN 전 | `19` `21` |
| 목록 UX | UX-ST1/ST0 **repo** | `39` · HUMAN |
| Match / embed | **OFF 유지.** ON 코드 있어도 켜지 말 것 | `#152` `#155` |
| 실DB 추출 | `sql_validate_wkf` HUMAN PASS. R6이 아님 | — |

버전 정본: `testWooAiStudioContext.jssp` `expectedByMod` + Studio URL `v=`. 이 문서·진행판의 옛 v=182를 배포표에 복사하지 말 것.

동결(열지 말 것): `testWooCompiler.js` SQL 조립 · `testWooGates.js` · EnPivot 도메인 few-shot · Match Option ON.

---

## 1-b. Phase Gate (매 파도 · Chat 시작 전 3분)

**목적:** 로드맵에서 **지금 하려는 키**가, 중간에 이미 넣어 둔 구현(#175 `_group`·#176 칩·#308 N월 바인드·Foundry 큐 규칙 등)을 **깨거나 우회하지 않는지** 확인한다.  
(“새 아이디어를 넣지 말라”가 아님 — **기존에 닫힌 동작을 회귀시키지 말라**.)

| # | 질문 | NO면 |
|---|---|---|
| G1 | PRD 불변식 유지? (LLM≠SQL · WF XML 금지 · Rhino · Match OFF) | 중단 |
| G2 | 현재 파도 가이드와 **같은 트랙**? (추출 vs 셸 섞지 않음) | Chat 분리 |
| G3 | **이미 PASS/HUMAN된 키** 동작 유지? (#175 M2G/M2C·IN 배열 · #176 칩=NL보강 · unresolved→Foundry 금지 · compiler 동결) | 설계 재검토·회귀 수정 |
| G4 | §2 `[S]` / 동결(compiler·Gates·EnPivot few-shot) **안 건드림**? | `[S]` 유지 |
| G5 | 선행 HUMAN·repo가 파도 표에 맞음? | 구현 금지 · HUMAN만 |
| G6 | diff가 **현재 파도 파일만**? | revert 또는 Chat 분리 |

**회귀 체크 예 (파도1):** `#176` 칩이 `_group` [맞음]·M2C 저장·Pass1 UNION을 대체하지 않음 · `#308` 1값 연월은 칩 없이 바인드 · `전라도`는 **generatePlan M2G/M2C** 경로(Foundry feasibility 아님).

**HUMAN 빈도:** 파도1 = 추출 회귀 **1회 묶음**(2~3문장). 파도2 = **R6 전후** 각 1회.

---

## 2. 큐에서 뺀 것 (파일은 삭제하지 않음)

운영자 08-11~13 원문 + 39 P제안 + R7 원문 중 **지금 제품에 필요 없는 구현**. `[S]` = 하지 말 것.

| ID | 원래 있던 것 | 왜 뺌 |
|---|---|---|
| 08-11 §1~6 | 캠페인 먼저 → WKF → SQL | **08-12 SQL-First가 대체.** R3~R6이 그 여정 |
| 유사 95% | Match 유사도 | #152 오탐. UX-ST0은 토큰 겹침. **Match ON 금지** |
| C1~C6 / Option ON | 값매칭 켜기 | OFF가 안정. 운영자 승인 전 금지 |
| #155 true | 임베딩 켜기 | OFF+영속화만. 재임베딩 배치 안 함 |
| S8 sessionStorage | R0 실측 | R2는 probe로 우회. 대기만, 구현 없음 |
| #175 G3 | enum에서 전남 삭제 실험 | 샘플 손상 |
| R7 relative SQL | compiler에 AddDays | 추출 손상. **배지+기존 clone만** (`21` 축소) |
| R7 신규 Duplicate | SOAP 실험 | `createWkfFromTemplate` 있음 |
| R8 지금 폐기 | Inject/Open/embed 폼 삭제 | 6개월+R6 안정 후. **지금 삭제 금지** |
| P2 | 해석 문장 LLM 추가 콜 | 칩 join으로 충분 |
| P4 | 계획 칩 추가/삭제 멀티턴 | **#176이 대체** (NL 보강 후 재생성) |
| P5 | 좋아요/별로 | 완성선 밖 |
| P6 | 건수 COUNT | **DEFERRED** 가이드. 구현 금지 |
| P3 pin | 목록 고정 | 검색은 UX-ST0. pin은 완성 후 |
| 코드맵 / request_queue | AGENTS deferred | 명시 요청 전 금지 |
| LangGraph/LangChain | 파이썬 에이전트 | ACC Rhino 불가. 패턴만 `#176` |
| Funnel COUNT / WF XML inject / LLM SQL | PRD 금지 | — |
| KO JOSA 사전 증식 | #174-5 no-op | 금지 |
| 도메인 few-shot EnPivot | 운영자 금지 | 금지 |

`[별도]_고도화_개발아이디어_관리자작성본.md` 원문은 **삭제하지 않음.** 08-13 EN-Pivot은 `#174 [x]`. 삼성 칩은 `#176`.

---

## 3. 남은 일 — 이 순서만

한 Chat = 한 행. 선행 HUMAN이 없으면 그 키의 **구현**은 하지 말고 운영자에게 테스트문만.

### 파도 1 — 추출 마감 (코드 허용 · **HUMAN 1회 묶음**)

| 순 | 키 | 하는 일 | 가이드 | 모드 |
|---|---|---|---|---|
| **1** | **#176** | Clarify Chips C1~C3 | `40` | **repo v=185 `[x]`** |
| 2 | **파도1 HUMAN** | `#308` + `#175-5` (+ C4-B는 샘플 없으면 SKIP) | `40` §7 · log #308 · `38` §5 | **HUMAN 1회** |
| — | #176 C4-A/B | 연월 칩·접두 3값 칩 | `40` §7 | A=1값이면 칩 없음 PASS · B=데이터 없으면 SKIP |

**파도1 HUMAN 입력 2문장 (코드 없음):**
1. `7월 미납자 남성` → SQL 청구월+미납+성별 · 연도 칩 없음  
2. `전라도에 사는 남성` → region IN · 칩 `전라도 → …` · [맞음] · SQL/멤버 변경 없음  

회신: `파도1 PASS/SKIP` + 두 SQL (+ C4-B SKIP 사유).

### 파도 2 — 셸 마감 (**코드 금지 · HUMAN 1~2회 묶음**)

repo 있음. **구현 Chat 없음** — 배포·클릭만. R6 직전·R6 각 1회 HUMAN.

| 순 | 키 | 하는 일 | 가이드 |
|---|---|---|---|
| 5 | #161 | 콘솔 JS lib → JSSP 재배포. litmus `v=` | `28` |
| 6 | **파도2 HUMAN-A** | R5 점프 + UX-ST1 + UX-ST0 (Match OFF) | `21` R5 · `39` §5 · P3 |
| 7 | **파도2 HUMAN-B** | **R6** 락→주입→덮어쓰기→[WKF 열기] | `21` R6 · `39` C-1 C-2 |

### 파도 3 — 축소 정리

| 순 | 키 | 하는 일 | 하지 말 것 |
|---|---|---|---|
| 10 | R7 | SQL 카드에 absolute 배지. 복제는 기존 clone | compiler relative, Duplicate SOAP |
| 11 | R8 | E2E 1회 ST0→ST6. 큐 정책 **문서**. intent 잔재 grep | Inject/Open 삭제, embed 폼 삭제 |

파도 3 전에 파도 2 HUMAN이 끝나야 한다.

---

## 4. 화면·기능 취합 (보완할 것 = 이미 키에 있음)

새 차수를 만들지 않음.

| 운영자/39가 말한 빈칸 | 흡수 |
|---|---|
| 모호하면 입력창 위 버튼, 삼성 전체+최신 멤버 | **#176** |
| 전라도 IN, 칩 별칭 | #175 · HUMAN 5단계 |
| 임시 SQL 클릭=이어서, Create 한 번 | UX-ST1 |
| 목록 검색 | UX-ST0 |
| WKF에 SQL 넣고 열기 | R6 |
| 동시 점유 락 | R6 soft-lock (repo) |
| 해석 칩 · SQL 접기 | U4 repo. #176이 모호 칩을 NL 위로 |

---

## 5. 다음 AI — 시작 방법

채팅 첫 메시지에 아래를 그대로 따라라.

```
1) Read 41 · 01 · 00_INDEX · SKILL · log Index 10
2) 지금 파도 = 1 HUMAN 묶음 (코드 금지) 또는 파도 = 2 (배포+HUMAN)
3) §1-b Phase Gate G1~G6 확인. 흡수표에 없는 아이디어 구현 금지
4) HUMAN은 AI PASS 금지. 파도 끝 1~2회만
```

### 금지 (매 Chat)

- LangGraph · Match Option ON · COUNT · G3 · EnPivot few-shot · compiler 동결 해제
- `docs/main/*` 무단 수정 · 문서+`new_ver` 한 커밋
- HUMAN PASS 자체 처리 · 구 0~9차 신규 구현 · §4 흡수표 밖 아이디어 구현
- 삼성/전라도/요금제 **리터럴 하드코딩**

### 답변 형식 (`00_INDEX` §3)

결과 3줄 + 배포 목록(해당 시) + HUMAN 테스트 + **다음 키**.

### #176 다음 키

**파도1 HUMAN 2문장** → PASS면 **파도2** (#161 배포 → HUMAN-A → HUMAN-B R6). 코드 Chat은 파도3(R7/R8)까지 없음.

---

## 6. 키 → 파일

| 키 | 파일 |
|---|---|
| #176 | `40_프롬프트검증_ClarifyChips_176.md` |
| #175 | `38_값확장_GroupExpansion_175.md` |
| R5~R8 | `21_SQLFirst_R차수_실행가이드.md` |
| UX / C-1~C-7 | `39_상용화_여정갭.md` |
| 여정 ST0~ST6 | `19_SQLFirst_여정정본.md` |
| 배포 대조 | `28_배포정합_진단.md` |
| 현황 | `01_진행판.md` |
