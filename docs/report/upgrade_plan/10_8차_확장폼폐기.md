# 8차 · 확장 폼 폐기 (for AI)

> **AGENT_MODE**. **6차 안정화 6개월 후**에만 착수.  
> UI-1-2 완결: Tools/Explorer Studio만 남긴다.

## 0. 실행 프롬프트

```
[#151 · 8차 확장폼폐기]
첨부: docs/report/upgrade_plan/10_8차_확장폼폐기.md
선행: 6차 PASS + 운영 안정화 6개월 증거(장애 티켓 없음 등 HUMAN 확인)
고유: 제거 대상 목록+롤백 경로 필수. 성급한 삭제 금지.
종료: acc-id-tracer + acc-verifier.
```

## 1. 차수 목표

WF 캔버스 임베드 진입점을 제거하고 Tools→Studio 경로로 통일한다(UI-1-2 완결).

## 2. 커버 ID

| ID | 원문 | 구현 |
|---|---|---|
| UI-1-2 | explorer로 최종 통일 | embed 폼/버튼 패치 제거 또는 비활성. Tools command만 공식 |

## 3. 선행 / 후속

- 선행: 6차 + 6개월
- 후속: **9차** `11_9차_미사용코드폐기정리.md` — 미사용 코드 `_폐기` 개명 + Campaign 콘솔 삭제(최종 청소)

## 4. 근거

- Navtree Tools commands (공식)
- 현행 제거 후보 파일 목록(아래). 최종 파일명 `_폐기` 처리는 9차에서 일괄.

## 5. 제거·변경 대상 파일 목록

| 경로 | 조치 | 롤백 보관 |
|---|---|---|
| `new_ver/input_form/testWooExtendWorkflow.xml` | 콘솔 연결 해제. repo는 8차에서 비활성·9차에서 `*_폐기` 개명 | git tag `pre-8차-embed` |
| `new_ver/workflow/testWooXtkWorkflowRedirectPatch.xml` | 패치 제거/역패치 → 9차 `_폐기` | 동일 태그 |
| `new_ver/workflow/testWooXtkWorkflowButtonPatch.xml` | 이미 DEPRECATED — 9차에서 `testWooXtkWorkflowButtonPatch_폐기.xml` | |
| `new_ver/js/testWooWorkflowUi.js` | Shell* 중 embed 전용만 축소. **Inject/Open에 쓰는 메서드는 유지** | |
| `new_ver/schema/testWooAiWorkflowUi.xml` | 미사용 메서드만 제거(신중) | |
| `new_ver/navtree/testWooAiNavtree.xml` | Studio command 유지·라벨 "공식"화 | |

## 6. 구현 상세

### Step 1 — 사용률 확인 (HUMAN)

- 최근 6개월 embed URL hit / 사용자 설문
- 미충족 시 착수 금지

### Step 2 — 역패치

- xtk:workflow 폼에서 AI Studio flatSubFormButton 제거
- package로 재배포

### Step 3 — 문서

- `01_개발가이드.md` 섹션7 embed 경로를 deprecated로 표시 — **사용자 요청 시에만** docs/main·report 본문 수정(본 차수 기본은 upgrade_plan 내 런북만)

### Step 4 — 회귀

- Tools Studio → 폴더→WKF→SQL→등록→열기 E2E

## 7. 금지사항

- Inject/Clone/Lock API까지 삭제
- 6개월 미충족 강제 삭제
- JSSP Studio 삭제

## 8. DoD

- [ ] WF Targeting 툴바에 AI Studio 버튼이 없다
- [ ] Tools → AI Studio로 E2E(폴더~등록)가 통과한다
- [ ] git tag `pre-8차-embed` 존재
- [ ] 롤백 절차대로 버튼 복구 리허설 1회 성공(스테이징)

## 9. HUMAN_CONSOLE

1. 안정화 기간 증빙 첨부
2. tag 생성
3. 역패치 배포
4. E2E + 롤백 리허설
5. `8차 HUMAN: …`

## 10. 테스트·디버깅

- [ ] 구 북마크 `embed=1` URL이 안내 페이지만 보여도 OK(또는 동일 Studio 동작)
- [ ] ShellBind가 Tools 플로우에서 여전히 동작

## 11. 롤백

1. `git checkout pre-8차-embed --` 대상 파일
2. form/workflow patch 재등록
3. Console 재시작

## 12. TBD

- 정확한 6개월 시작 일자(6차 배포일)

## 13. 종료 게이트

UI-1-2 완결 표시 + tracer PASS.
