# SQL-First 전환 · 자산 판정표 (#146R · 작업2)

> **문서 전용**. 조사 결과(`12_PoC-S`)가 최초 판정안과 다르면 **조사 우선**.  
> 원 차수 = 기능을 처음 구현·가이드한 차수 번호.

판정: **유지** / **개조** / **폐기** / **신규**

---

## 1. 서버 · 스키마

| 자산 | 원 차수 | 판정 | 사유 |
|---|---|---|---|
| `new_ver/js/testWooWorkflowClone.js` (복제·락·상한·CreateInstanceFromModel) | 3 | **유지** | ST5/ST6에서 재사용. 락 호출 시점만 R6에서 “반영 직전”으로 이동 |
| `new_ver/js/testWooFragments.js` | 기존/~4 | **유지** | Stage A 검색 불변 |
| `new_ver/js/testWooLifecycle.js` | 기존/5 | **유지** | normalizeSql · sqlContentHash · publish/revoke |
| `new_ver/js/testWooCompiler.js` | 기존 | **유지** | CNF→SQL. R7에서 relative 표현 시 개조 가능 |
| `new_ver/js/testWooLlm.js` | 기존 | **유지** | Pass0/Pass1 |
| `new_ver/js/testWooGates.js` | 기존 | **유지** | 게이트 |
| `new_ver/js/testWooFoundry.js` | 4 | **유지** | 자동승인 경로. 잔여물 정리는 R8 |
| `new_ver/js/testWooRepository.js` | 기존/5 | **개조** | 반영 시점 기록·커밋 트랜잭션 지원. `listAiSqlForMatch`/`saveAiSql`/`findAiSqlBySqlHash` 유지 |
| `new_ver/js/testWooMatch.js` | 5/#146/#147 | **개조** | 탐색 주경로 승격. discover를 containment Top-10·intersection≥1로 정본 정렬(#147R). dedup 모드 회귀 0 |
| `new_ver/js/testWooConfig.js` / `testWooEnv.js` / `testWooCommon.js` | 기존 | **유지** | |
| `schema/testWooAiSql.xml` | 기존 | **유지** | workflow_name nullable 확인됨(S4). SF 전환만으로 스키마 필수 변경 없음 |
| `schema/testWooAiFragment.xml` | 기존 | **유지** | |
| `schema/testWooAiRequestQueue.xml` | 4 | **유지** | R8 정리 정책 대상 |
| `schema/testWooAiWkfLock.xml` | 3 | **유지** | 커밋 단계에서 엄격 사용 |

---

## 2. JSSP · API

| 자산 | 원 차수 | 판정 | 사유 |
|---|---|---|---|
| `testWooAiGenerate.jssp` | 기존/#147 | **유지**(미세개조 가능) | 컨텍스트 없이 동작(S1). `plan_only` 유지(S2) |
| `testWooAiValidate.jssp` | 기존 | **유지** | |
| `testWooAiRegister.jssp` | 기존/5/6 | **개조** | ST6 커밋 트랜잭션화(#150R). 주입+기록+락 |
| `testWooAiMatch.jssp` | 5/#146/#147 | **개조** | `mode` 통과 · discover 응답 필드 정본화 |
| `testWooAiStudioContext.jssp` | 2/3 | **개조** | 호출 순서: 폴더/캠페인/WKF는 SQL 확정 후. selectWkf 락은 조회 soft-fail 유지 vs 커밋 엄격 분리 |
| `testWooAiStudio.jssp` | 1 | **개조** | 셸 레이아웃 · 브레드크럼 · litmus v= |
| `testWooAiStudioJs.jssp` + `html/testWooAiStudio.js` | 1/2/5/#147 | **개조** | 상태머신 ST0~ST6 · 캐시 · intent 제거 |
| Fragment Review/Admin JSSP | 4/7 | **유지** | 7차 중복관리 경로 |

---

## 3. UI · 폼 · 내비

| 자산 | 원 차수 | 판정 | 사유 |
|---|---|---|---|
| navtree Tools 진입 (`testWooAiNavtree.xml`) | 1 | **유지** | |
| 0차 폼 colspan·캐시헤더 결론 | 0 | **유지** | #133/#135/#137/#141 회귀 금지 |
| `input_form/testWooExtendWorkflow.xml` embed | 기존/1 | **유지**(8차까지) | embed=1 회귀 금지. 8차에서 폐기 예정은 그대로 |
| intent create/reuse 버튼·분기 (#147) | #147 | **폐기** | SQL 목록이 분기 대체 (`13` §2) |
| “폴더 선택 전 입력 비활성” (UI-4-1) | 2 | **폐기**(정책) | SQL-First에서 ST0 즉시 입력. 스펙드리프트 기록 |
| CAMPAIGN→WKF→SQL 순서 전제 UI-2-5/6 | 2 | **개조** | SQL→프로그램→캠페인→WKF로 반전 |
| Jaccard≥0.9 dedup 경로 | 5 | **유지** | mode=dedup 기본값 · 임계 변경 금지 |
| discover containment==1.0 · Top-20 (#147) | #147 | **개조** | 정본: intersection≥1 · Top-10 |

---

## 4. 문서 · PoC

| 자산 | 원 차수 | 판정 | 사유 |
|---|---|---|---|
| `01`~`11` 차수 가이드 | 0~9 | **SUPERSEDED** (삭제 금지) | 상단 배너만. 실행 라우팅은 R0~R8 |
| `04_3a_PoC*` RESULT | 3a | **유지** | CreateInstanceFromModel 등 사실 근거 |
| `17_[완료]_PoC-M_매칭의미론.md` | #145 | **유지** | 값인식·복제 문서 근거. SQL-First와 병행 |
| `18_[완료]_PoC-S_SQLFirst타당성.md` | #145R | **유지** | 전환 게이트 |
| `13` / `14` / `15` | #146R | **신규** | 정본·판정·실행 |
| intent 분기 설계 메모(#147 Chat) | #147 | **폐기** | 목록 분기 대체 |

---

## 5. 신규 자산 (구현 차수)

| 신규 | 담당 R | 설명 |
|---|---|---|
| 캐시 계층 (`twStudioCache`) | R2 | 미반영 SQL · 휘발 규칙 |
| ST0~ST6 상태머신 | R3 | 클라이언트 phase 재정의 |
| 단일 목록 패널 + 브레드크럼 | R4 | 셸 재구성 |
| 커밋 트랜잭션 | R6 | 락→주입→기록→해제 |
| 미반영/동일/relative 배지 | R3/R7 | UI |
| 큐 잔여물 정책·정리 | R8 | 문서 또는 배치 |

---

## 6. 손실·보존 요약

| 영역 | 손실? |
|---|---|
| WorkflowClone / Fragments / Lifecycle / Foundry 자동승인 / 7차 중복관리 | **거의 없음** |
| Match / Register / Studio 셸·상태머신 | **개조** (재작성 아님) |
| intent UI · 캠페인선지정 입력게이트 · discover==1.0 | **폐기/개조** |
| 서버 라이브러리 전면 rewrite | **불필요** |
