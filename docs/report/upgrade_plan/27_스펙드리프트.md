# 98. 스펙 드리프트 기록

> 원문(`[별도]_고도화_개발아이디어_관리자작성본.md`) · 추적표 · 구현 정본 사이의 **의도적 차이**만 기록한다.  
> ID 삭제·병합·재번호 금지. 차이 사유를 남겨 회귀 논쟁을 방지한다.

---

## D-1. 여정 순서 반전 (SQL-First) — 2026-08-12

| 항목 | 내용 |
|---|---|
| **드리프트 ID** | D-1 |
| **원문** | 2026-08-11 §4~5·§9: 캠페인 폴더 선지정 → 입력 활성 → (WKF 먼저 또는 조건 먼저) → SQL |
| **정본** | 2026-08-12 추가 수정 + `19_SQLFirst_여정정본.md`: **조건 → SQL목록 → (신규\|기존) → 프로그램/캠페인/WKF → 반영** |
| **차이** | 원문 4~5번은 “캠페인 폴더 선지정”을 입력의 전제로 둔다. SQL-First는 그 전제를 폐기하고 저장 위치를 결과 이후로 미룬다. |
| **영향 ID** | UI-4-1, UI-4-3, UI-4-5, UI-2-5, UI-2-6, J-9-1(시점), FLOW-5-*, UI-4-7/8 |
| **신규 차수** | R0~R8 (`21_SQLFirst_R차수_실행가이드.md`) |
| **승인** | 관리자 원문 2026-08-12 + Agent 분석(intent 분기 폐기 · 락 점유 단축 · 매칭 코퍼스 청정) |

### 원문 4~5번과의 명시적 차이

1. **입력 게이트**: 원문 “최초 진입 시 입력창 비활성화 → 폴더 지정 후 활성” → 정본 “ST0에서 즉시 입력 활성”.
2. **목록 주연**: 원문 오른쪽은 CAMPAIGN/WKF/SQL이 컨텍스트에 따라 전환 · SQL은 WKF 종속. 정본은 SQL 목록이 **첫 결과 화면**이고 프로그램/캠페인/WKF는 그 다음.
3. **유사도 95%**: 원문(08-11 §5, 08-12 §2) 수치 유지 요구 → 정본 **기각**. containment Top-10 + 동일 배지 (`12_PoC-S`, VLDB). FLOW-5-3의 Jaccard≥0.9는 **dedup 모드 전용**으로 잔존.
4. **새 캠페인 생성**: 원문 §3 “새 캠페인 생성 없음” → 08-12는 분기 A에서 캠페인 생성/선택 허용. **08-12가 우선**(드리프트). UI-3-3는 “목록에서의 무분별 생성 금지”로 재해석하고, ST4 의도적 생성은 허용.
5. **intent create/reuse (#147)**: 08-12 이전 Agent 제안 → SQL-First에서 **폐기**(목록이 분기).

### 구현 지연 (정본 유지 · 코드가 뒤처짐) — 2026-08-13

가이드가 08-12 원문으로 **되돌아간 것이 아니다**. R3는 ST0 입력 활성만 repo에 넣었고, `register()`의 WKF 필수 에러는 구여정 잔존이었다.

| 원인 | 내용 |
|---|---|
| `21` R4 DoD가 얇음 | table/px·브레드크럼 높이만 적고, ST1 SQL목록·**[등록]≠WKF선행**은 `19` §2에만 있음 |
| 파이프라인 선행 | #164~#172(슬롯삼킴·원자화·FragContract)가 R4보다 먼저 들어가 셸 개편이 밀림 |

해소: R4 repo **v=160** — Tools [등록] → SQL 목록(새 SQL / 미반영 draft). 분기 B 점프·ST6 DB 커밋은 R5/R6. Match OFF라 기존 SQL 유사 목록은 비어 있음(승인 전 ON 금지).

---

## D-2. discover 임계 (구현 vs 정본) — 2026-08-12

| 항목 | 내용 |
|---|---|
| **드리프트 ID** | D-2 |
| **구현(#147)** | `valueContainment == 1.0` · `DISCOVER_TOP_N=20` |
| **정본(#147R / 13)** | `intersectionCount >= 1` · Top-10 · 비율 임계 없음 |
| **해소 차수** | R1 |

---

## D-3. 미반영 SQL 저장 위치 — HUMAN

| 항목 | 내용 |
|---|---|
| **드리프트 ID** | D-3 |
| **원문 08-12** | 캐시 · 창 종료 시 휘발 |
| **제약** | `sessionStorage` 가용성은 urlViewer `documentMode` 의존 (`12_PoC-S` S8) |
| **해소** | R0 HUMAN → R2 구현 분기 |

---

## D-4. 매칭 정확도 결함 · 기능 차단 — 2026-08-12 (#152)

| 항목 | 내용 |
|---|---|
| **드리프트 ID** | D-4 |
| **원문** | §5·08-12 §2 “유사도 95% 이상” 목록 |
| **사고** | name 집합 매칭이 서울≠인천을 “포함”으로 단정 (`22_매칭오탐_사고기록.md`) |
| **조치** | Option `testWooAiMatchEnabled` **기본 OFF** · Studio v=154 · Match API disabled 응답 |
| **원문 95%와의 관계** | 임계 상향으로는 해결 불가(값 동일 name 집합 Jaccard 1.0). #154 CONFLICT 배제·값 복합키로 대체 충족 |
| **SQL-First** | R1 매칭 승격은 #154 재개 전에 메인 노출 금지. 차단이 선행 |
| **해소 차수** | #153 PoC-V → #154 구현 · 사용자 승인 후 Option ON |

---

## D-5. EN-Pivot 좌표계 전환 — 2026-08-13 (#174)

| 항목 | 내용 |
|---|---|
| **드리프트 ID** | D-5 |
| **원문** | 2026-08-13: 한국어 표층 매칭 대신 번역 후 EN 공간에서 param 추출·매칭 |
| **정본** | `37_ENPivot_CanonicalLayer_174.md` · 바인딩은 항상 `db` 원본 값 · `nlMap` 병존 |
| **차이** | 구현(#234/#235)은 `JOSA_TAIL`·렉시콘 최장일치로 한국어 표층을 깎았다. 정본은 그 **증식을 폐기**하고 EN pivot을 주경로로 둔다. KO 사전 코드 삭제는 **#174-5 repo**. |
| **재개** | #170 Pass0 · #168-B 바인딩 · #172 매칭. #167 도메인 형상·#169-P0 히트 술어는 #174-3/4에서 보강 |
| **폐기(전략)** | 조사/어미 사전 증식 · #170 S5 KO 축힌트 주경로 · nlMap 수기 주좌표계 · V7 비용 게이트 |
| **여정** | ST0~ST6(D-1)는 유지. 바뀌는 것은 ST0 Generate **내부** 좌표계 |
| **승인** | 관리자 원문 2026-08-13 + 확정 설계 블록 |

### 트랙 분리

1. **트랙 A 추출**이 최우선 (08-12 “조건 뽑기” + 08-13 번역). `#174` 0단계는 코드 금지.
2. **트랙 B 셸** R5 배포 HUMAN은 병렬. R6에 추출 우회 구현 금지.