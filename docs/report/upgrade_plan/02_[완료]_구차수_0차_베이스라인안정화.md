# 0차 · 베이스라인 안정화 (for AI)

> **[SUPERSEDED BY 19/20/21]** SQL-First 전환(2026-08-12). 실행 라우팅은 `00_INDEX.md` 키 `R0`~`R8` / 가이드 `21_SQLFirst_R차수_실행가이드.md`. 본 문서는 삭제하지 않으며 역사·자산 근거로만 참조한다. 정본 여정: `19_SQLFirst_여정정본.md`.

> **AGENT_MODE**: 이 문서를 Chat에 첨부하면 너는 구현 Agent다.  
> **코드 신규 기능 금지.** #141 베이스라인 검증·문서화·필요 시 최소 핫픽스만.  
> 다른 차수 문서 내용을 이 파일에 넣지 마라.

## 0. 실행 프롬프트 (복붙)

```
[#143 · 0차 베이스라인안정화]
첨부: docs/report/upgrade_plan/02_[완료]_구차수_0차_베이스라인안정화.md
선행: docs/report/11_고도화_추적표.md, .cursor/rules/00-acc-guardrails.mdc
규칙: 신규 기능 금지. 폼 colspan·캐시헤더·v= 리트머스만. #133/#135/#137/#141 되돌리기 금지.
종료 전 acc-verifier + acc-id-tracer.
```

## 1. 차수 목표

#134~#139 회귀를 막고, 현재 embed Studio가 흰화면 없이 열리는 **검증된 기준선**을 고정한다.  
고도화 UI/상태머신/WKF 생성은 착수하지 않는다.

**브랜치 전제**: `docs/roadmap-v2`에서 `git diff main -- new_ver/` 가 **비어 있어야** 한다. 비어 있지 않으면 0차 DoD를 시작하지 말고 코드/문서 분리부터 한다. 레이아웃 후보 코드는 `fix/embed-layout`에만 둔다.

## 2. 커버 ID

| ID | 원문/의미 | 이 차수 구현 형태 |
|---|---|---|
| (#141) | 폼 그리드 좌측공백 — Reload 버튼 colspan=3 | `testWooExtendWorkflow.xml`에서 colspan 합 검증. 깨졌으면 복구만 |

(추적표 기능 ID 없음 — 베이스라인 게이트)

## 3. 선행조건 / 후속 영향

- 선행: 없음
- 막히면: 1차 이후 전부 착수 금지 (흰화면 위에 셸을 올리면 원인 분리 불가)

## 4. 어도비/프로젝트 근거

- Form structure: https://experienceleague.adobe.com/en/docs/campaign-classic/using/configuring-campaign-classic/input-forms/form-structure
- 프로젝트 실패사: log #133(fetch/Promise), #135(캐시), #137(FormatDate), #141(colspan)

## 5. 변경 파일 목록

| 경로 | 신규/수정 | 요지 |
|---|---|---|
| `new_ver/input_form/testWooExtendWorkflow.xml` | 수정(필요 시만) | colspan/enter/`_r` 정합 |
| `new_ver/jssp/testWooAiStudio.jssp` | 수정(필요 시만) | `v=` 상수·no-cache 헤더 확인 |
| `new_ver/jssp/testWooAiStudioJs.jssp` | 수정(필요 시만) | IE 금지 API 재유입 검사 |
| `new_ver/html/testWooAiStudio.js` | 동기 | StudioJs와 동일 유지 |
| `docs/log/log.md` | 수정 | 본 차수 완료 로그 |

## 6. 구현 상세 (무엇을/어디에/왜)

### Step A — 정적 회귀 스캔 (Agent)

1. **무엇을**: `new_ver/jssp/testWooAiStudioJs.jssp` + `html/testWooAiStudio.js`에서 `fetch|Promise|classList|forEach|\.finally|confirm\(|alert\(` 검색  
   **어디에**: 위 파일  
   **왜**: #133 재발 방지
2. **무엇을**: Studio CSS에 `flex|grid|vh|var\(--|calc\(` 검색  
   **왜**: #134/#130 재발 방지
3. **무엇을**: 폼 XML에 `FormatDate|GetDate` 검색  
   **왜**: XTK-170016 (#137)
4. **무엇을**: Reload/Apply 버튼 `colspan`이 부모 `colcount`와 합 일치하는지 확인  
   **왜**: #141

위반이 있으면 **최소 diff**로만 복구. 레이아웃 재설계 금지.

### Step B — 버전 리트머스

- Studio URL/쿼리에 보이는 `v=` 값을 DoD에 명시된 숫자와 맞출 것 (현재 코드 기준 예: v=143 전후 — **실제 파일 상수 읽고 기록**).
- 폼 `urlViewer` src에 `v=` 와 `_r=` 전달되는지 확인.

### Step C — HUMAN_CONSOLE 대기

Agent는 아래 체크를 완료 처리하지 말고, 운영자 결과를 입력받아 기록.

## 7. 금지사항 (차수 고유)

- 캠페인/WKF 목록, 정보바, 상태머신, Spawn, fragment 승인 변경 금지
- #133/#135/#137/#141 동작 되돌리기 금지
- “개선” 목적의 CSS/JS 리팩터 금지

## 8. DoD (화면 관찰 가능)

- [ ] WF Targeting → AI Studio 버튼 → urlViewer에 Studio 제목 문구가 보인다
- [ ] urlViewer가 흰 빈 화면이 아니다
- [ ] Studio URL(또는 소스)에 `v=` 파라미터가 보인다
- [ ] 좌측 입력/생성 영역과 우측 SQL 이력(워크플로우명 있을 때)이 동시에 보인다
- [ ] 폼 하단 Reload/Apply 행에 큰 좌측 공백이 없다 (#141)
- [ ] 클라이언트 JS에 `fetch`/`Promise` 문자열이 없다 (파일 검색 0건)

## 9. HUMAN_CONSOLE (운영자)

1. Client Console 재시작(또는 로컬 캐시 클리어)
2. 스키마/JS 변경이 있었다면: **schema → js → form** 순 재배포
3. 테스트 WF 열고 AI Studio → 흰화면 여부 스크린샷/메모
4. Tools 메뉴의 기존 Studio command가 있으면 동일 확인(실패해도 0차 FAIL 아님 — 1차 범위)
5. 결과를 Chat에 붙여넣기: `0차 HUMAN: PASS|FAIL + 메모`

## 10. 테스트·디버깅 체크리스트

### 정적

- [ ] rg 금지 API 0건
- [ ] Studio.jssp ↔ StudioJs.jssp ↔ html/js 삼자 동기(임베드 스크립트 경로 확인)

### 런타임 (HUMAN)

- [ ] embed=1 진입
- [ ] workflowName 있는/없는 두 케이스
- [ ] Apply 후 urlViewer 재로드 시 흰화면 여부 (#143)

### 디버그 순서 (실패 시)

1. URL에 `v=` 없는가 → JSSP 미배포/캐시
2. `v=` 최신인데 흰화면 → 클라이언트 JS 예외(금지 API)
3. 폼만 깨짐 → colspan/enter
4. XTK-170016 → FormatDate 잔존

## 11. 롤백

- git에서 본 차수 커밋만 revert
- 콘솔: 직전 정상 form/JSSP 패키지 재등록
- 베이스라인 커밋 해시/로그 번호를 `docs/plan/0차_plan.md`에 기록

## 12. TBD

- 없음 (기능 TBD 없음)

## 13. 종료 게이트

```
/acc-id-tracer → EXPECTED empty-or-(#141) covered
/acc-verifier → READY only if DoD all PASS or BLOCKED(human) with no FAIL
```
