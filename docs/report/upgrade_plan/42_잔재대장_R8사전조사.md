# 42 — Studio/JSSP 잔재 대장 (R8 사전조사)

> **모드:** grep·라인 증거만. 코드 변경 없음. 조치시점 고정값 준수.

| ID | 대상(파일:라인) | 종류 | 증거 | 조치시점 | 제거 시 위험 |
|---|---|---|---|---|---|
| **S-1-06** | `testWooAiStudioJs.jssp:534-568` · `testWooAiStudioContext.jssp:61-67,108` | **배포정합** | mismatch 없을 때 diag `libs: allMatch expected=159`(568) — 실제는 `expectedByMod`(llm=187 foundry=170 fragContract=186 …). diag 출력 keys(534-537)에 **toolkit·feasibility·probe·dedup·workflowUi 누락**. `allMatch`는 libs 전체(77-98) 대 expectedByMod 검사 — PASS여도 누락 모듈·오표시 expected=159로 **배포 정합 오판** 가능 | **S-1 (R8-03보다 우선)** | libVersions PASS 신뢰 상실 · stale 모듈 방치 |
| R8-01 | `testWooAiStudio.jssp:387` vs `testWooAiStudioJs.jssp:3678-3682` | UX중복 | HTML `#hint` `banner ok` 「자연어로… Y요금 1020세대…」. Shell `init()`만 `banner warn` 「조건을 입력한 뒤 [대상 만들기]…」로 덮음. embed(`SHELL_MODE=false`) init 성공 경로는 `#hint` 미덮음(3692-3707, `#hint`는 3699-3702 loadSqlList **오류 시만**) | S-1 | embed에서 HTML 기본값 제거·hidden 시 안내 소실 |
| R8-02 | `testWooAiStudio.jssp:330-335` · CSS 214-215 | UX중복 | `#twBootTable`/`#twBoot` HTML `class="tw-boot-show"`. CSS 기본 `display:none`, tw-boot-show 시 block. `init()` 3623-3631에서 `display:none` | 유지 | JS 미로드 시 urlViewer 진단 단서 상실 (#133~#136) |
| R8-03 | `testWooAiStudioJs.jssp:3,44` · `:3616` vs `testWooAiStudio.jssp:17,24,332,351,435-436` | 문서drift | Studio URL·TW-BOOT **v=187** (6건). StudioJs 헤더 주석 **v=185** (2건). init diag **v=181** (1건). (`expectedByMod` __v 축과 별개) | S-1 | litmus·diag 문자열만 불일치(기능 무관) |
| R8-04 | `testWooAiStudio.jssp:234-237,393` · `#funnel` | dead DOM | DOM `#cardFunnel`+`#funnel`. CSS `table.funnel` 4규칙. `renderFunnel` grep **0건** | R8 | P6 COUNT 재개 시 UI 재작성 필요 |
| R8-05 | `testWooAiStudioJs.jssp:3575` | dead fn | `hideFunnel` 정의 1건. 호출 979,1069 + `_restoreDraft` 285 — **총 3호출**. 항상 `#cardFunnel` hidden | R8 | generate 성공 후 funnel 표시 경로 없음(현재도 dead) |
| R8-06 | `testWooAiStudioJs.jssp:996` | dead fn | `discoverFind` 정의 1건(jssp). `discoverFind(` 호출 grep **0건** (new_ver 전체). `runMatch(` 호출 2건(1070,2821) — discoverFind와 별개 | 유지 | Match Option ON 복구 경로 소실(진행판 §3 삭제 금지) |
| R8-07 | `testWooAiValidate.jssp:39,180-200` | dead API | `action=count` 분기·`_runCount` 스텁(`funnel:[]`). 클라이언트 `action:"count"` grep **0건**. `testWooAiValidate.jssp` 호출 9건 모두 validate/listRecentNl/pushRecentNl/getSql/deleteSql/setBindPick | R8 | P6 COUNT 재개 시 API 재설계 |
| R8-08 | `testWooAiStudioJs.jssp:3463,3444` · `#chips` | dead fn | `removeChip` 정의 1·onclick 1. `#nlHints`/`renderNlHints`(835)와 DOM 분리. removeChip→`state.plan` 직접 편집→`validate()`(3494). renderNlHints→`generate({clarifyPick})`(856) | R8 | plan 칩 × 제거·재검증 UX 상실(#176 NL보강과 정책 충돌) |
| R8-09 | `testWooAiStudio.jssp:256` `.alt-chips` | dead DOM | CSS `.alt-chips` 1건. HTML `#alt-chips` **0건**. `#nlHints` 칩은 class `alt-chip`(848) | R8 | 스타일만 dead |
| R8-10 | `testWooAiStudio.jssp:405` `#twNlHint` | UX중복 | composer 위 hidden 기본. `setInputEnabled(false)` 시 표시(1878-1887). Shell `#hint` init 문구(3681)와 유사 | R8 | busy 표시 수단 상실 |
| R8-11 | `testWooAiStudioJs.jssp` vs `html/testWooAiStudio.js` | 문서drift | `function` 선언명 Compare-Object **차이 0**. 줄수 jssp **3729** / html **3720**. html 헤더 v=185(35), diag v=181(3607) | R8 | 40 C3 DoD 3종 동기 — S-1 시 3종 동시 수정 필수 |
| R8-12 | `testWooAiMatch.jssp` 전체 | dead API(휴면) | Option OFF 시 disabled 응답(49-59). Studio `runMatch` 1219 정의·1070,2821 호출(MATCH_ENABLED 가드) | 유지 | Match ON 운영자 승인 경로 |
| R8-13 | `testWooLlm.js` splitCompound (파도1 HUMAN debug) | 관찰 | `7월 미납자 남성` → splitCompound 3축 후 slot 1·3 «7월» 중복. `decide matched=3`이라 SQL 결과는 정상 | R8 | 슬롯 중복 시 Pass1/Foundry 비용·혼란 |
| R8-14 | Studio 해석 칩 라벨 (#176/38) | 관찰 | 파도1 PASS SQL `2026-07, 1, M` — unpaid `1`이 마케터可读 라벨 없이 DB 원값 노출 | #176 C4·38 §5 | 칩/요약 라벨 축 개선 전까지 UX 부채 |

---

## R8 조치 완료 (log #325 · 2026-08-24)

| ID | 조치 | 비고 |
|---|---|---|
| **S-1-06** | `[x]` log #321 | libVersions 21모듈·expectedByMod |
| **R8-03** | `[x]` v=189 동기 | Studio·StudioJs·diag |
| **R8-04** | `[x]` | `#cardFunnel` DOM·funnel CSS 제거 |
| **R8-05** | `[x]` | `hideFunnel` 제거 · hideResultCards에서 cardFunnel 제외 |
| **R8-07** | `[x]` | Validate `action=count`·`_runCount` 제거 |
| **R8-09** | `[x]` | `.alt-chips` dead CSS 제거 |
| intent | `[x]` grep 0 | 실행 경로 잔재 없음(헤더 주석 2건만) |

**유지(삭제 금지):** R8-06 discoverFind · R8-08 removeChip · R8-12 Match.jssp · Inject/Open/`testWooWorkflowUi`

---

## Foundry 큐 운영 절차 (R8 · `19` §7 구현)

| 항목 | 정책 |
|---|---|
| 사용자 약속 | 미반영 SQL은 `testWooAiSql` 미기록 — 마케팅 이력에 남지 않음 |
| 서버 현실 | Foundry 큐(`testWooAiRequestQueue` 보류) · Studio 폴링 최대 **10분** · stale **30분** |
| 운영 점검 | 주 1회 `testWooAiQueueStatus.jssp` 또는 GapAdmin — `queued`/`processing` 장기 행 확인 |
| stale 처리 | **30분 초과 `queued`** → Foundry WF 로그·GapLog 확인 후 수동 재큐 또는 fragment 수동 승인 |
| 창 종료 | 폴링 중 닫아도 큐 행 잔존 가능 — TTL 자동 삭제 **없음** (`12_PoC-S` S7) |
| 코드 배치 | **미구현** — 6개월+R6 안정 후 검토. 지금은 운영 절차만 |

---

## R8 HUMAN — PASS (log #333 · 2026-08-24)

| 항목 | 결과 |
|---|---|
| E2E | ST0→ST6 1회 PASS · «7월+iOS+LTE» SQL 생성 |
| 회귀 | Generate · absolute 배지 · R6 경로 유지 |
| 파도3 | **닫힘** — 신규 구현 Chat 없음 |
