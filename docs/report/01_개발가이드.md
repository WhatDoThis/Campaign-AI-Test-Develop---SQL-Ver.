# AI 대상자 추출 시스템 — 개발 가이드 (Test Woo)

`v1.4.3` · 2026-08-03 · Adobe Campaign v7 · **테스트 적용본**

근거: `docs/main/PRD.md` v1.6 · 스캐폴드: `new_ver/` (코어 정합 완료)

---

## 설계 전제

| 목표 | 대응 |
|---|---|
| fragment ~100,000 | Stage A 슬롯별 검색 → Pass1이 후보만 선택 |
| 모호한 NL | Pass0 LLM 슬롯 분해 + Pass1 merge/split + sample_questions/synonyms |
| 조건 10개+ | 조합 계획 → set-op 컴파일러 |
| 외부 연동 최소화 | outbound ≈ LLM API만 |
| 과기능 금지 | codemap / request_queue / 퍼널 COUNT = DEFERRED |

---

## 파이프라인

```
NL → Pass0(LLM 슬롯분해) → Stage A(슬롯별 검색)
   → Pass1(LLM 선택·재분할·조합계획) → set-op 컴파일러
   → 최소검증 → Register(ai_sql_id) → 커스텀 액티비티가 런타임 조회·실행
```

> WF XML에 SQL을 직접 쓰지 않는다. Register는 이력만 남기고, 액티비티는 `ai_sql-id`로 조회한다.

---

## 네이밍

| 대상 | 규칙 | 예시 |
|---|---|---|
| 로컬 파일명 | `testWoo` 카멜 | `testWooAiFragment.xml`, `testWooConfig.js` |
| 네임스페이스 | `woo` | |
| 스키마 name | `testWoo` 카멜 | `testWooAiFragment` |
| **sqltable** | **스키마 name과 동일** | `sqltable="testWooAiFragment"` |
| **입력 폼 name** | **스키마 name과 동일(카멜)** | `testWooAiFragment` |
| 폼 entity-schema | `woo:` + 스키마 name | `woo:testWooAiFragment` |
| 폼 / 스키마 label | 영어 표시명 | 식별자와 별개 |
| JS Code 엔티티 | `testWoo` 카멜 | `woo:testWooFragments.js` |
| 옵션·권한 | `testWoo` 시작 | `testWooAiLlmApiKey` |
| URL | `/woo/` | `/woo/testWooAiStudio.html` |

> Explorer 폴더 뷰는 기본적으로 **스키마와 같은 name의 입력 폼**을 암시 사용한다.  
> 스키마·폼·로컬 파일·JS 식별자를 **카멜로 통일**.

---

## 섹션 1 — 기반 세팅 (지금 적용)

캠페인 콘솔에서 **권한 + LLM 옵션만** 등록한다. 스키마/JS/JSSP는 섹션 2+.

### 1) 명명된 권한

경로: `Administration > Access management > Named rights`

| Internal name | Label (영어) | 필수 |
|---|---|---|
| `testWooAiSqlGenerate` | Test Woo AI SQL Generate | ✅ 필수 |
| `testWooAiSqlRegister` | Test Woo AI SQL Register | ✅ 필수 |
| `testWooAiLibraryManage` | Test Woo AI Library Manage | 권장 (fragment 관리·향후) |

테스트용 오퍼레이터/오퍼레이터 그룹에 위 권한을 부여한다.

### 2) 옵션 (초기 필수 = LLM 3개만)

경로: `Administration > Application settings > Options` (환경에 따라 Platform > Options)

| 옵션명 (Internal name) | 권장 Value | 비고 |
|---|---|---|
| `testWooAiLlmProvider` | `openrouter` | **선택(기본 openrouter).** `anthropic` \| `openrouter`. 롤백 시 `anthropic` + 아래 Endpoint/Model/ApiKey를 직결 값으로 되돌리면 코드 변경 없이 복귀 |
| `testWooAiLlmApiKey` | (필수 · 연동 시) | JS 기본값 없음. **OpenRouter 키** (`sk-or-…`). 비우면 LLM 호출 실패. **XtkOption은 평문** — 실키는 `nms:extAccount`+`decryptPassword` 권장 |
| `testWooAiLlmModel` | `anthropic/claude-opus-5` | JS 기본값 없음. OpenRouter 모델 슬러그 |
| `testWooAiLlmEndpoint` | `https://openrouter.ai/api/v1/chat/completions` | JS 기본값 없음. OpenRouter Chat Completions |
| `testWooAiPass0Examples` | (비움 OK) | **선택.** Pass0 도메인 키워드 예시. 비우면 추상 규칙만 |
| `testWooAiAllowedCidr` | (비움 OK) | **선택.** 쉼표 구분 IP 접두. `logon()`이 127.0.0.1로 존을 우회하므로 API 보완 게이트 |
| `testWooAiLlmUseProxy` | (비움/`0`) | **선택.** `1`이면 `HttpClientRequest.execute(true,…)` 프록시 (`getConfig().llm.useProxy`) |

> **Anthropic 직결 롤백 값 (참고):**  
> `testWooAiLlmProvider=anthropic` · `testWooAiLlmEndpoint=https://api.anthropic.com/v1/messages` ·  
> `testWooAiLlmModel=claude-sonnet-4-6`(또는 기존 모델) · Anthropic API Key

> **아웃바운드 URL 허용 (필수 · basis):** Campaign 서버가 `HttpClientRequest`로 LLM을 호출하려면  
> `serverConf.xml` 의 `<urlPermission>`에 엔드포인트가 허용돼 있어야 한다.  
> 미등록 시 `JST-310026`. 변경 후 **nlserver web 재시작** 필요.  
> 기존 `<urlPermission>` 블록의 `action` 속성은 건드리지 말고 `<url>` 한 줄만 추가:
> ```xml
> <!-- OpenRouter (기본) -->
> <url dnsSuffix="openrouter.ai" urlRegEx="https://openrouter\.ai/api/v1/.*" />
> <!-- Anthropic 직결 롤백용 (유지 권장) -->
> <url dnsSuffix="api.anthropic.com" urlRegEx="https://api\.anthropic\.com/.*" />
> ```
> **Hosted/Hybrid 배포**면 Control Panel → URL permissions 또는 **Adobe 지원 티켓**으로 허용 요청.  
> On-premise면 `serverConf.xml` 수정 후 `nlserver web` 재시작.  
> 사내 프록시면 그 호스트 허용 + `testWooAiLlmEndpoint`·`testWooAiLlmUseProxy=1`.  
> **주의:** `logon(sessionToken)`은 요청 IP가 아닌 127.0.0.1로 로그온되어 Authorized connection zone을 우회한다.  
> Studio/API는 Cookie+`logon` 정공법이나, 존 정책은 `testWooAiAllowedCidr`로 보완한다.

### 3) 섹션 1에서 만들지 말 것 (삭제·제외된 항목)

아래는 **예전에 안내했으나 v1.2+에서 초기 필수에서 제거**됨. 만들지 않는다.

| 제외 항목 | 이유 |
|---|---|
| `testWooAiMaxConditions` | 10+ 조건 허용. 하드캡 과설계 |
| `testWooAiScaleMaxPct` | 규모 게이트 후순위 |
| `testWooAiExplainCostMax` | EXPLAIN 게이트 후순위 |
| `testWooAiAllowedSchemaRoot` | CD/스키마 루트와 무관. 과설계 |
| codemap / request_queue 관련 옵션·권한 추가 | DEFERRED |

### 4) 섹션 1 완료 체크

- [ ] 권한 2개 필수(+ LibraryManage 권장) 생성·할당  
- [ ] LLM 옵션 등록: `testWooAiLlmProvider` + ApiKey + Model + Endpoint (키는 나중에 가능)  
- [ ] `serverConf.xml` urlPermission에 `openrouter.ai` 허용 (+ Anthropic 롤백용 유지 권장)  
- [ ] 이름에 `uplus` 없음 · `testWoo` / `woo` 규칙 준수  

---

## 섹션 1 — 이전 안내 대비 변경점

| 구분 | 이전(초기 과설계 / 초기 가이드) | 현재 (v1.4.1) |
|---|---|---|
| 권한 | Generate / Register / LibraryManage 3개 동등 필수처럼 안내 | **Generate·Register 필수**, LibraryManage는 **권장** |
| 옵션 | LLM 3개 + MaxConditions / ScaleMaxPct / ExplainCostMax / AllowedSchemaRoot | **Provider + LLM 3개** (나머지 **삭제·만들지 않음**) |
| 네임스페이스 | 한때 `uplus` 예시 | **`woo` / `testWoo*` 만** |
| 스키마·JS 배포 | 섹션1에 섞여 안내된 적 있음 | **섹션1 = 권한·옵션만**. 스키마는 섹션2 |
| 퍼널/무거운 게이트 옵션 | 초기에 포함 검토 | **제외** |

---

## 섹션 2 — 데이터 모델 (지금 적용)

스키마 2개 + 입력 폼 2개만 배포한다. **JS/JSSP/HTML은 섹션 3.**

### 소스 파일

| 캠페인 객체 | namespace | 로컬 파일 |
|---|---|---|
| Data schema | `woo:testWooAiFragment` | `new_ver/schema/testWooAiFragment.xml` |
| Data schema | `woo:testWooAiSql` | `new_ver/schema/testWooAiSql.xml` |
| Input form | `woo:testWooAiFragment` (name=스키마와 동일) | `new_ver/input_form/testWooAiFragment.xml` |
| Input form | `woo:testWooAiSql` (name=스키마와 동일) | `new_ver/input_form/testWooAiSql.xml` |

### 배포하지 않음 (DEFERRED)

| 파일/스키마 | 이유 |
|---|---|
| `testWooAiCodemap.xml` / `woo:testWooAiCodemap` | 보류 |
| `testWooAiRequestQueue.xml` / `woo:testWooAiRequestQueue` | 보류 |
| 대응 input_form | 보류 |

### 적용 순서 (Campaign 콘솔)

1. **소스 스키마 등록**  
   `Administration > Configuration > Data schemas`  
   - New (또는 Import XML) → `testWooAiFragment.xml` 내용 붙여넣기  
   - namespace=`woo`, name=`testWooAiFragment`, label 영어 확인  
   - Save  
   - 동일하게 `testWooAiSql.xml` → `woo:testWooAiSql`

2. **DB 구조 업데이트**  
   스키마 저장 후 환경 절차에 따라 Update database structure / SQL 생성·적용  
   - 물리 테이블: `testWooAiFragment` / `testWooAiSql` (`sqltable` = 스키마 name)  
   - 인덱스: fragment `byName`(unique, key 암묵) + `idx_cat_status` / sql `idx_wf`  
   - `status`는 schema enum → 폼 드롭다운 (`active|deprecated` / `draft|validated|registered|rejected`)

3. **입력 폼 등록** (스키마 저장·DB 반영 후)  
   `Administration > Configuration > Input forms`  
   로컬 XML (`new_ver/input_form/…`)을 Import하거나 내용 붙여넣기.

   | 항목 | Fragment 폼 | SQL 이력 폼 |
   |---|---|---|
   | namespace | `woo` | `woo` |
   | **name (식별자)** | `testWooAiFragment` | `testWooAiSql` |
   | entity-schema | `woo:testWooAiFragment` | `woo:testWooAiSql` |
   | label | Test Woo AI Condition Fragment | Test Woo AI Generated SQL |

   - name은 **스키마 name과 반드시 동일** (Explorer 암시 연결)  
   - `<input>`는 **`xpath`만** 사용. `height` / `type="text"` 넣지 말 것 (`xtk:form`에 없음 → XML-110013)  
   - memo 필드는 스키마 `type="memo"`면 멀티라인으로 **자동** 표시  
   - 예전에 스네이크 name(`test_woo_ai_*`) 스키마·폼이 있으면 **폐기 후** 최신 카멜 XML로 재등록

4. **탐색기 폴더(선택 · 섹션2 후반)**  
   Navigation hierarchy에서 폴더 타입을 둘 때 `<view schema="woo:testWooAiFragment" …>` 이면  
   폼 `woo:testWooAiFragment`가 **자동** 연결된다 (`form=` 오버라이드 불필요).  
   예: `Test Woo AI / Fragments`, `Test Woo AI / SQL History`

5. **스모크 (권장)** — fragment 샘플 1건 수기 등록 후 목록·상세 폼 조회

### fragment 필드 요약 (`woo:testWooAiFragment`)

| 필드 | 필수 | 설명 |
|---|---|---|
| `name` | ✅ | 안정 ID (unique) |
| `label` | ✅ | 표시명 |
| `category` | ✅ | Stage A 필터 |
| `tags` | ✅ | 검색 태그 |
| `synonyms` | 권장 | JSON/텍스트 동의어 |
| `key_column` | ✅ | grain (CD 링크 키와 동일해야 함) · **물리 컬럼명**(예 `sCustomer_id`) |
| `sql_text` | ✅ | **완전한 SELECT** (`{{param}}` 허용) · 테이블·컬럼 모두 **물리명** |
| `params` / `param_domain` | ✅ | JSON 스펙 |
| `description` / `sample_questions` | ✅ | 검색·LLM 힌트 |
| `status` | ✅ | enum `fragmentStatus`: `active` / `deprecated` |

### 이력 필드 요약 (`woo:testWooAiSql`)

| 필드 | 설명 |
|---|---|
| `nl_request` | 원문 NL |
| `plan_json` | 조합 계획 JSON (**구 `ir_json` 아님**) |
| `sql_query` | 최종 SQL |
| `status` | enum `sqlHistoryStatus`: draft / validated / registered / rejected |
| `workflow_name` | 주입 대상 WF 인터널네임(`WKF94` 등). 정수 `@id`는 패키지 이동 시 바뀌므로 쓰지 않는다 |

### 샘플 fragment 1건 (스모크용 · key/SQL은 환경에 맞게 수정)

| 필드 | 예 |
|---|---|
| name | `age_band` |
| label | Age band |
| category | `demo` |
| tags | `age,1020,세대` |
| key_column | *(환경 grain 물리명, 샘플 기준 `sCustomer_id`)* |
| status | `active` |
| sample_questions | `["1020세대","10대 20대","10~20대"]` |
| synonyms | `["1020","MZ어린"]` |
| params | `{"min":{"type":"number"},"max":{"type":"number"}}` |
| param_domain | `{"min":{"required":true},"max":{"required":true}}` |
| sql_text | `SELECT DISTINCT t.sCustomer_id AS sCustomer_id FROM testWooSampleCustomer t WHERE t.iAge BETWEEN {{min}} AND {{max}}` |

> 실제 테이블·컬럼명은 **고객 마트 기준**으로 바꾼다. 잘못된 SQL을 넣지 말 것.

### 논리명 vs 물리명 — `sql_text` 작성 필수 규칙

`sql_text` / `key_column` 은 원시 SQL 이므로 **물리명만 통한다.** ACC 는 스키마
속성에 `sqlname` 이 없으면 타입 접두사를 붙여 물리 컬럼명을 자동 생성한다.
논리 속성명을 그대로 쓰면 `column does not exist` 로 실패한다.

`woo:testWooSampleCustomer` 실측값 (2026-08-06 배포 스키마 기준):

| 논리 속성명 | 물리 컬럼명 | 타입 |
|---|---|---|
| `id` (PK) | `iTestWooSampleCustomerId` | long |
| `customer_id` | `sCustomer_id` | string |
| `age` | `iAge` | long |
| `region` / `gender` / `status` / `email` / `name` | `sRegion` / `sGender` / `sStatus` / `sEmail` / `sName` | string |
| `marketing_consent` / `sms_consent` | `iMarketing_consent` / `iSms_consent` | byte |
| `birth_date` / `created_date` | `tsBirth_date` / `tsCreated_date` | date/datetime |

접두사 규칙: `s`=string, `i`=정수/byte/boolean, `ts`=날짜·시각, `d`=double,
`m`=memo. PK 는 `i{PascalCase스키마명}Id`.

| 구분 | 논리명 사용 | 물리명 사용 |
|---|---|---|
| `xtk.queryDef` / `xtk.session.Write` / input form `xpath` | ✅ | ✗ |
| `sql_text` · `key_column` · `sqlSelect` · `sqlGetInt` · `populationCountSql` | ✗ | ✅ |

**테이블명은 `sqltable` 선언값 그대로다.** 네임스페이스 접두사를 붙이지 않는다.
`WooTestWooSampleCustomer` 같은 이름은 존재하지 않는다.

**물리명 확인 방법.** 추정하지 말고 툴킷 `describe_schema` 의 `sqlColumn` 값을
쓴다. 이 값은 배포된 확장 스키마의 `@sqlname` 이라 항상 실제 컬럼과 일치한다.
`probe_values` 는 논리명·물리명 어느 쪽을 넘겨도 내부에서 물리명으로 해석하지만,
`probe_sql` 과 최종 `sql_text` 는 호출자가 물리명을 써야 한다.

**행 수 주의.** ACC 는 테이블마다 PK=0 기술 레코드 1건을 유지한다. 그래서
`SELECT COUNT(*)` 는 `queryDef operation="count"` 보다 항상 1 크다
(샘플 고객 101 vs 100). `populationCountSql` 의 분자(`probe.total`)도 원시 SQL 이라
같은 기준이므로 `COUNT(*)` 를 그대로 쓴다.

### 섹션 2 완료 체크

- [ ] `woo:testWooAiFragment` / `woo:testWooAiSql` 스키마 존재  
- [ ] DB 테이블 `testWooAiFragment` / `testWooAiSql` · 인덱스 생성됨  
- [ ] 입력 폼 2개 연결됨 (name=스키마 카멜)  
- [ ] codemap / request_queue **없음**  
- [ ] `expr` / `ir_json` / `target_table` 필드 **없음** (`sql_text` / `plan_json`만)  
- [ ] (권장) active fragment 샘플 ≥ 1건  

### 섹션 2 — 이전 안내 대비 변경점

| 구분 | 이전 | 지금 |
|---|---|---|
| 스키마 개수 | fragment + sql + **codemap** + **request_queue** | **fragment + sql 만** |
| fragment SQL 필드 | `expr` (WHERE 조각) | **`sql_text` (SELECT 전체)** |
| 제거된 fragment 필드 | `target_table`, `join_key`, `scope_default` | **만들지 않음** |
| 추가 필드 | — | `tags`, `synonyms` |
| 이력 JSON | `ir_json` | **`plan_json`** |
| 스키마·폼 name | 스네이크 `test_woo_ai_*` | **카멜** `testWooAiFragment` / `testWooAiSql` |
| sqltable | 자동명 또는 별칭 | **스키마 name과 동일** |
| 네임스페이스 | `uplus` 가능 | **`woo`** |

이미 옛 스네이크 스키마·폼을 만들었다면 **폐기 후** 위 규격으로 재등록한다.

---

## 섹션 3 — JS 라이브러리 (다음 적용)

서버사이드 `JavaScript Codes`만 배포한다. **JSSP/HTML은 섹션 4.**

### 배포할 파일 (7개 · 순서 권장)

경로(콘솔): `Administration > Configuration > JavaScript codes`  
로컬: `new_ver/js/`

| 순서 | Campaign 이름 (`namespace:name`) | 로컬 파일 | 역할 |
|---|---|---|---|
| 1 | `woo:testWooCommon.js` | `testWooCommon.js` | jsonOut / readPayload / requireRight |
| 2 | `woo:testWooConfig.js` | `testWooConfig.js` | LLM 옵션 + **queryDef 가드레일** |
| 3 | `woo:testWooFragments.js` | `testWooFragments.js` | Stage A (페이지 5000, sql_text 일괄금지) |
| 4 | `woo:testWooLlm.js` | `testWooLlm.js` | Pass0 / Pass1 **CNF plan** · OpenRouter/Anthropic 프로바이더 어댑터 |
| 5 | `woo:testWooCompiler.js` | `testWooCompiler.js` | CNF → INTERSECT/UNION/EXCEPT |
| 6 | `woo:testWooGates.js` | `testWooGates.js` | 최소 검증 |
| 7 | `woo:testWooRepository.js` | `testWooRepository.js` | 이력 저장 → **id 반환** |

### 배포하지 않음
- `testWooCodemap.js` — DEFERRED  
- `testWooWorkflowIo.js` — **폐기** (WF XML Write 금지). 등록=`ai_sql_id`, 액티비티 런타임 조회

### ACC 서버 JS (Rhino) 허용·금지 — 공식 API 기준

서버 `JavaScript Codes` / JSSP 서버 블록은 **브라우저 ES6가 아님**. Adobe Campaign Classic 내장 Rhino + Campaign API만 사용.

| 구분 | 허용 (문서/실전) | 금지 (이 프로젝트에서 사용 중단) |
|---|---|---|
| 배열 | `for` 루프, `.length`, `.push`, `.slice`, `.join`, `.sort(fn)` | `Array.map` / `forEach` / `filter` / `every` / `some` / `find` / `includes` |
| 문자열 | `indexOf`, `substring`, `replace`, `split`, `toLowerCase` | `String.trim` (미보장) → `replace(/^\s+\|\s+$/g,"")` |
| HTTP | `HttpClientRequest` + **동기** `execute([hasProxy])`, `fromString(str,"utf-8")`(요청·문자열명), `body.toString()` / `toString(CODEPAGE_UTF8)`(**응답·정수**) | `body.toString("utf-8")` 문자열 codePage(빈 body), `HttpClientRequest.wait` |
| 인증 | `logon(sessionToken)`, `application.operator`, `getUTF8Parameter` | 커스텀 Host 기반 SOAP Logon |
| 기타 | `JSON.parse/stringify`, E4X `for each`, `getOption`, `xtk.queryDef`, `xtk.session.Write`, `application.getDBMSType` | `Promise` / `async`/`await` / `=>` / `` ` `` / `const`·`let` / `fetch` (서버) |

> **함정:** Java 브리지에서 `typeof HttpClientRequest.wait === "function"`이 참이어도 **호출 시** `is not a function`이 난다.  
> → `wait`는 **참조조차 하지 않는다**. LLM 호출은 동기 `execute`만.

스튜디오 **브라우저** JS(`testWooAiStudioJs.jssp`)는 외부 Chrome/Edge 전제라 `fetch` 사용 가능. ACC 콘솔 내장 IE에서는 동작하지 않음.

### 조합 계획 (CNF) · 등록 계약

```json
{
  "grainKey": "customer_id",
  "include": [ { "any": [ { "fragment": "…", "params": {} } ] } ],
  "exclude": [ { "fragment": "…", "params": {} } ],
  "unmatched": []
}
```

- Register → `{ ok, ai_sql_id }` (워크플로우 XML 변경 없음)  
- 커스텀 액티비티 속성: `ai_sql-id` → `woo:testWooAiSql`에서 `sql_query` 로드·실행

### ACC queryDef 가드레일 (필수 인지)

| 규칙 | 값 |
|---|---|
| `lineCount` 미지정 시 | 약 **10000**행만 / 또는 그 규모로 메모리 피크 | 
| 권장 페이지 | **5000** (`testWoo.cfg` 상한과 동일) |
| Stage A | `sql_text` **미포함** 검색 + `startLine` 페이지 |
| `getByName` | `lineCount=1` 단건 (전체 로드 금지) |
| 근거 | Adobe KCS KA-28003 등 |

이미 `testWooFragments.js`를 옛 `loadAll` 버전으로 넣었다면 **반드시 최신 파일로 교체**.

### JS 전역 객체 (스키마 namespace와 분리)

| 구분 | 식별자 | 예 |
|---|---|---|
| 스키마 / `loadLibrary` namespace | `woo` | `woo:testWooAiFragment`, `loadLibrary("woo:testWooConfig.js")` |
| 서버 JS 전역 객체 | **`testWoo`** | `testWoo.cfg`, `testWoo.llm`, `testWoo.compiler` … |

> ACC에서 `woo.cfg`는 JS 속성이 아니라 스키마 `woo:cfg` 조회로 해석된다(XFR-180000).  
> 전역 객체 이름은 스키마 namespace와 **반드시 다르게** 유지한다.

### 등록 요령

1. New JavaScript code  
2. **namespace** = `woo`  
3. **name** = `loadLibrary("woo:…")`와 **문자열 일치** (예: `testWooCommon.js`)  
4. 본문 붙여넣기 → Save  
5. **반드시 1→7 순서** (`Config`를 `Fragments`/`Llm`보다 먼저)

> JS **name은 카멜**. 스키마/폼과 표기만 맞추면 되고 식별 문자열은 서로 독립.

### 섹션 3 완료 체크

- [ ] 7개 식별키 = `loadLibrary` 문자열  
- [ ] Fragments가 **페이지네이션 버전**(loadAll 전량 로드 없음)  
- [ ] Codemap / WorkflowIo **미배포**  
- [ ] Compiler가 CNF(`include`/`exclude`)만 받음  
- [ ] Config가 Fragments보다 먼저 로드되는 구조 이해  
- [ ] 전역 객체가 `testWoo.*` (구 `woo.*` 아님)  

---

## 섹션 4 — JSSP + Studio UI (`/woo/`)

브라우저 UI와 JSON API를 **Dynamic JavaScript Pages**로 배포한다.  
네임스페이스 `woo` → URL 접두사 **`/woo/`** (별도 web alias 설정 불필요).

### 전제

- 섹션 1~3 완료 (권한 · 스키마 · JS 7개)
- 로그인 세션으로 브라우저 접근 (`credentials: include`)
- 운영자 권한: `testWooAiSqlGenerate` (생성/검증), `testWooAiSqlRegister` (등록)

### 콘솔 경로

`Administration > Configuration > Dynamic JavaScript Pages`  
(환경에 따라 `Configuration > Dynamic JavaScript pages`)

### 배포할 페이지 (5개 + 진단 1)

로컬: `new_ver/jssp/`

| 순서 | Name (namespace=`woo`) | URL | 역할 | 권한 |
|---|---|---|---|---|
| 1 | `testWooAiGenerate.jssp` | `/woo/testWooAiGenerate.jssp` | NL → CNF → SQL | Generate |
| 2 | `testWooAiValidate.jssp` | `/woo/testWooAiValidate.jssp` | validate / count / catalog | Generate |
| 3 | `testWooAiRegister.jssp` | `/woo/testWooAiRegister.jssp` | 이력 → `ai_sql_id` | **Register** |
| 4 | `testWooAiStudio.jssp` | `/woo/testWooAiStudio.jssp` | UI shell | 세션(`logon`) |
| 5 | `testWooAiStudioJs.jssp` | `/woo/testWooAiStudioJs.jssp` | UI script | — |
| (진단) | `testWooAiAuthDebug.jssp` | `/woo/testWooAiAuthDebug.jssp` | login/쿠키 진단 | 임시 |

### 배포하지 않음

| 파일 | 이유 |
|---|---|
| `testWooAiLogon.jssp` | **폐기** (Host 인젝션·서버 IP Logon). 스텁만 로컬 — 콘솔에 있으면 삭제 |
| `testWooAiCatalog.jssp` | Validate `action=catalog`로 흡수 |
| `testWooAiCount.jssp` | Validate `action=count`로 흡수 |
| `testWooAiCodemapSync.jssp` | DEFERRED |
| Workflow XML inject | 없음 — Register는 `ai_sql_id`만 |

이미 Logon/Catalog/Count를 콘솔에 만들었다면 **삭제하거나 비활성**.

### 등록 요령

1. New Dynamic JavaScript Page  
2. **Namespace** = `woo`  
3. **Name** = 표의 Name과 **완전 일치** (확장자 `.jssp` 포함)  
4. 로컬 파일 본문 전체 붙여넣기 → Save  
5. Publish 없음 — Save 즉시 URL 접근  
6. Studio: **4·5** 배포. `Studio.jssp` → `StudioJs.jssp` 로드  
7. JS 라이브러리도 재배포: `testWooCommon` / `Gates` / `Compiler` (인증·게이트·NULL wrap)

> Studio 로직 원본: `new_ver/html/testWooAiStudio.js`  
> 수정 시 html과 `jssp/testWooAiStudioJs.jssp`를 **같이** 맞춘 뒤 재붙여넣기.

> **인코딩:** Client Console에 한글 UTF-8을 그대로 붙여넣으면 UI가 `????`로 깨질 수 있다.  
> `testWooAiStudio.jssp`는 한글을 `&#x…;` 엔티티로, `StudioJs` 런타임 문자열은 `\uXXXX`로 둔다.  
> 로컬 `html/*.html`은 읽기용 한글 유지 가능 — **배포본은 반드시 jssp 파일**을 붙여넣기.

> **브라우저:** Studio는 **외부 브라우저 새 창**을 권장. ACC 콘솔 내장(IE)은 `fetch`/`URLSearchParams`/`Promise.finally` 미지원.

### API 계약 (요약)

공통: `ok` = 서버 처리 성공, `passed` = 게이트 통과. AUTH → `{ ok:false, code:"AUTH", logonUrl }`.

| Endpoint | Body | 성공 |
|---|---|---|
| Generate | `{ nl_request, workflow_name? }` | `{ ok, passed, plan, sql, chips, summary, results }` |
| Validate | `{ action:"validate", plan }` | `{ ok, passed, results, sql, summary, chips, plan }` |
| Validate | `{ action:"count", plan }` | `{ ok, sql, keyColumn, funnel:[], … }` |
| Validate | `{ action:"catalog" }` 또는 `?action=catalog` | `{ ok, categories }` |
| Register | `{ plan, title, nl_request, … }` (**sql 무시**) | `{ ok, passed, ai_sql_id, status, sql, summary }` |

- Content-Type: `application/x-www-form-urlencoded`  
- 필드: `payload` = UTF-8 JSON (`getUTF8Parameter`)  
- Register: `X-Requested-With: TestWooStudio` + Origin/Referer  
- unmatched 시 Generate: `{ ok:false, unmatched:[…], plan }` (창작 없음)

### Studio 접속 · 웹 인증 (ACC v7)

```
https://<campaign-host>/woo/testWooAiStudio.jssp
https://<campaign-host>/woo/testWooAiStudio.jssp?workflowName=<internalName>
https://<campaign-host>/woo/testWooAiAuthDebug.jssp   ← 진단 후 삭제
```

- 미인증 → `/nl/jsp/logon.jsp?target=…Studio`  
- JSSP/API: Cookie `__sessiontoken` → 공식 `logon(sessionToken)`으로 운영자 바인딩  
  (`sessionTokenOnly=false` 존에서 쿠키만으로는 Anonymous인 문제 해결)  
- CSRF(Register): `X-Requested-With: TestWooStudio` + Origin/Referer host 일치  
- Ref: [logon()](https://experienceleague.adobe.com/developer/campaign-api/api/f-logon.html), [Security zones](https://experienceleague.adobe.com/en/docs/campaign-classic/using/installing-campaign-classic/additional-configurations/security-zones)

- **생성** → Pass0→StageA→Pass1 CNF → compile → gates(active fragment)  
- **등록** → plan 재컴파일 SQL만 저장 → `ai_sql_id`  
- WF XML **미변경** (섹션 5 · 커스텀 액티비티 `ai_sql-id`)

### 스모크 체크

- [ ] AuthDebug: `sessionTokenPresent` + `loginAfterLogon` 비어 있지 않음  
- [ ] Studio URL + `StudioJs.jssp` 200, 헤더에 `user: <login>`  
- [ ] Validate catalog: `payload={"action":"catalog"}` → `{ ok:true, categories:… }`  
- [ ] Generate: 한글 NL이 깨지지 않음 (`getUTF8Parameter`)  
- [ ] Generate: Journal/로그에 `[testWoo.llm] usage:` — `reasoning_tokens` 0 확인 (OpenRouter)  
- [ ] Generate: 잘못된 model 슬러그 → Studio 빨간 배너에 API error 메시지  
- [ ] Generate: `testWooAiLlmProvider=anthropic` 롤백 시 기존 Anthropic 직결 동작 유지  
- [ ] Generate: 권한 없으면 `code:"FORBIDDEN"`  
- [ ] Register: 클라이언트 `sql` 보내도 서버 재컴파일 결과만 저장  
- [ ] Register → Explorer `woo:testWooAiSql` + `ai_sql_id`  

### 섹션 4 이후

| 섹션 | 내용 |
|---|---|
| **5** | 샘플 스키마·시드 · Studio NL 라운드트립 |
| **6** | Foundry 선행조건 · 스모크 |
| **7** | WF 캔버스에서 Studio 열기 · WF별 SQL 목록 · 커스텀 액티비티 `ai_sql-id` E2E |

---

## 섹션 5 — 샘플 고객·가입 (E2E용)

> **DB에 DDL/INSERT SQL을 직접 실행하지 않는다.**  
> 캠페인 콘솔: **srcSchema 등록 → Update database structure → 워크플로우 JS Run**

### 소스 파일

| 캠페인 객체 | namespace | 로컬 파일 |
|---|---|---|
| Data schema | `woo:testWooSampleCustomer` | `new_ver/schema/testWooSampleCustomer.xml` |
| Data schema | `woo:testWooSampleSubscription` | `new_ver/schema/testWooSampleSubscription.xml` |
| Input form | `woo:testWooSampleCustomer` | `new_ver/input_form/testWooSampleCustomer.xml` |
| Input form | `woo:testWooSampleSubscription` | `new_ver/input_form/testWooSampleSubscription.xml` |
| WF 스크립트 | *(붙여넣기)* | `new_ver/workflow/testWooSampleSeed.js` (생성기 출력) |

시드 스크립트 재생성 (로컬):

```powershell
cd new_ver\sample_data
python generate_testwoo_sample_seed.py
# → new_ver/workflow/testWooSampleSeed.js 갱신 → WF JavaScript code에 다시 붙여넣기
```

### 적용 순서 (Campaign 콘솔)

1. **소스 스키마 등록** — 섹션2와 동일  
   `Administration > Configuration > Data schemas`  
   - `testWooSampleCustomer.xml` → Save  
   - `testWooSampleSubscription.xml` → Save  

2. **Update database structure**  
   - 물리 테이블: `testWooSampleCustomer` / `testWooSampleSubscription`  
   - **외부 DB에 CREATE TABLE SQL 직접 실행 금지**

3. **입력 폼 등록** (선택 · Explorer 조회용)

4. **워크플로우 생성**  
   `Administration > Production > Technical workflows` (또는 Campaign workflows)  
   - **Start** → **JavaScript code** → **End**  
   - JavaScript code activity 더블클릭 → `testWooSampleSeed.js` **전체 붙여넣기**  
   - 상단 `var CLEAR = true` — 재적재 시 true, 최초 1회만 false 가능  

5. **Run** → Journal에 `[testWooSampleSeed] done … count(cust=100,sub=150)` 확인

### 샘플 데이터 규칙 (생성기 보장)

- 고객 100건 · 가입 150건 — `(customer_id, plan_code)` 유일
- 탈퇴 후 재가입 → 다른 요금제 / 동시기간 다중 요금제 허용

### 섹션 5 샘플 스키마 완료 체크

- [ ] 스키마 2개 + Update database structure  
- [ ] WF Run → 고객 100 · 가입 150  
- [ ] (다음) fragment 샘플 등록 → Studio E2E  

---

## 섹션 6 — Foundry 배포 선행조건 (WKF_testWooFoundry)

Foundry(미매칭 슬롯 → fragment 자동 생성)는 **읽기 SQL 실행 권한**과 **단일 실행 보장**이 전제다.
둘 중 하나라도 빠지면 조용히 오작동하거나 중복 fragment가 생성된다.

### 1) 오퍼레이터 `sql` named right — **필수**

경로: `Administration > Access management > Operators` (또는 Operator groups)

- **`WKF_testWooFoundry` 를 실행하는 오퍼레이터에 `sql` named right 필수.**
- 근거: 공식 문서 `sqlGetInt` — *"The operator must have the 'sql' right to use this function,
  otherwise an exception is raised."*
  ([sqlGetInt](https://experienceleague.adobe.com/developer/campaign-api/api/f-sqlGetInt.html))
- 권한이 없으면 `testWoo.probe.preflight()` 가 `NO_SQL_RIGHT` 로 실패하고,
  배치는 **큐 상태를 바꾸지 않고 중단**된다(재시도 가능). 오류 ID 접두사 `FFPERM`.
- 확인: WF JavaScript code 액티비티에서

```javascript
loadLibrary("woo:testWooProbe.js");
logInfo(JSON.stringify(testWoo.probe.preflight()));
```

  → `{"ok":true}` 가 아니면 권한 부여 후 재확인.

### 2) 워크플로우 동시 실행 금지 (단일 인스턴스)

경로: WF 열기 → `Edit > Properties`(또는 속성 아이콘) → **Execution** 탭

- **"Do not launch if the workflow is already running"**(동시 실행 금지) 계열 옵션을 켠다.
  버전에 따라 라벨이 다르므로 *이미 실행 중이면 새 인스턴스를 만들지 않는* 설정을 선택.
- 스케줄러는 5분 주기. 이전 배치가 길어져도 새 인스턴스가 겹치지 않게 한다.
- 코드 측 2중 가드: `processBatch()` 가 `processing` 레코드 존재 시 `skipped:true` 로 빠지고,
  30분 이상 정체된 `processing` 은 `queued` 로 되돌린다(`testWooEnv.js` → `foundry.staleProcessingMinutes`).
- **완전한 원자성은 이 WF 설정에 의존한다.** `_claimQueue` 의 재조회 검증만으로는
  두 인스턴스 동시 진입을 막지 못한다.

### 3) 배포 순서 (반드시 이 순서)

**스키마 재등록 → DB 구조 업데이트 → JS library 배포 → JSSP 배포 → navtree**

| 순서 | 대상 | 비고 |
|---|---|---|
| 1 | Data schemas 재등록 | `testWooAiFragment` / `testWooAiSql` / `testWooAiRequestQueue` / `testWooAiGapLog` |
| 2 | **Update database structure** | 신규 컬럼이 물리 테이블에 반영돼야 함 |
| 3 | JavaScript codes | 로드 순서 `Env → Config → … → Foundry` |
| 4 | JSSP (`/woo/`) | Studio·Generate·Validate·Register·QueueStatus·FragmentReview |
| 5 | navtree | 확장 컬럼(`@version`/`@active`/`@dedup_verdict`)은 1~2 완료 후에만 |

> 2번을 건너뛰고 JS만 배포하면 queryDef 가 XTK 스키마 오류로 실패한다.
> `_getQueue` 는 이 오류를 **"스키마 배포가 선행되지 않았습니다"** 메시지로 치환해 던진다.

### 4) Foundry 확장 필드 배포 점검

| 스키마 | 확인할 필드 |
|---|---|
| `woo:testWooAiRequestQueue` | `plan_json` · `workflow_name` · `partial_preview` · `slot_results` · `evidence_log` · `clarify_answers` · `tokens_used` · `cost_estimate` |
| `woo:testWooAiFragment` | `scope_key` · `status` · `version` · `is_current` · `content_hash` · `emb_vector` · `emb_source_hash` · `usage_count` · **`dedup_verdict`** · **`dedup_match_id`** · **`dedup_diff_count`** |
| `woo:testWooAiSql` | `used_fragments` · `compile_hash` · `impact_status` · `excluded_slots` |

- 신규 3필드(`dedup_*`)는 dedup `near` 판정 이력용. `near` 도 publish 하지만
  큐는 `awaiting_approval` 로 남고 승인 화면에서 기존 fragment 와 비교 검토한다.
- attribute 는 `dbindex`/`key` **하단**에 배치한다 (로그 #22 규칙).

### 5) ACC 읽기 SQL 규약 (코드 계약)

| 함수 | 규약 |
|---|---|
| `sqlSelect(format, query)` | 1번째 인자는 **라벨이 아니라 결과 XML 스키마** `"docName,[fieldXPath:type[:length],]*"`. 반환은 **XML 객체**(배열 아님) → `.length` / `[0]` 접근 금지, `for each` 순회 ([문서](https://experienceleague.adobe.com/developer/campaign-api/api/f-sqlSelect.html)) |
| SELECT 별칭 | 모든 프로브 쿼리는 `AS tw_key` / `AS tw_val` 처럼 **명시적 별칭**을 주고 format 과 1:1 대응 |
| `sqlExec` | **전면 금지.** 조회는 `sqlSelect`/`sqlGetInt`, 레코드 CRUD 는 `xtk:session#Write` |
| `application.getSchema` | 스크립트 종료까지 메모리 유지 → `search_columns` 는 namespace당 **30개** 상한, 초과 시 `partialScan:true` ([문서](https://experienceleague.adobe.com/developer/campaign-api/api/m-Application-getSchema.html)) |
| XML 속성 파싱 | 정규식 금지, **E4X** 사용 (ACC 스키마는 속성 순서 자유 · `label` 생략 가능) |
| `application.getSchema` 반환형 | **Schema 클래스**이며 공식 메서드는 `toDocument()` 하나뿐. `toXMLString` 은 DOMElement 메서드이므로 `sch.toDocument().documentElement.toXMLString()` 을 경유한다. Schema 에 직접 호출하면 런타임 예외 ([Schema](https://experienceleague.adobe.com/developer/campaign-api/api/c-Schema.html) · [toDocument](https://experienceleague.adobe.com/developer/campaign-api/api/m-Schema-toDocument.html)) |
| `queryDef` `getIfExists` | **엘리먼트 자체**를 반환한다(무매치는 빈 엘리먼트). `select` 만 `<xxx-collection>` 으로 감싼다 → `res.<root>[0]` / `.length` / `for each` 금지. 존재 판정은 `String(res.@id \|\| "") === ""`, `<select>` 에 `<node expr="@id"/>` 필수 ([문서](https://experienceleague.adobe.com/developer/campaign-api/api/sm-queryDef-ExecuteQuery.html)) |
| 물리 컬럼명 | **추정 금지 · 조회는 필수.** `sqlname` 미지정 시 물리명은 타입 접두사 + 이름으로 자동 생성된다(`customer_id` → `sCustomer_id`). 유일한 정답은 **배포된 확장 스키마의 `@sqlname`** 이며, 툴킷 `describe_schema` / `search_columns` 가 `sqlColumn` 으로 노출한다. `sqlColumn` 이 빈 값이면 SQL 컬럼이 아니다(XML 저장 필드) → 조회 불가. 상세 대응표는 섹션 2 "논리명 vs 물리명" 참조 ([문서](https://experienceleague.adobe.com/en/docs/campaign-classic/using/configuring-campaign-classic/schema-reference/database-mapping)) |
| 물리 테이블명 | **`sqltable` 선언값 그대로.** 네임스페이스 접두사를 붙이지 않는다(`WooTestWooSampleCustomer` 는 없음). PostgreSQL 은 미인용 식별자를 소문자로 접기 때문에 오타 시 소문자 이름으로 오류가 난다 |
| 기술 레코드 | ACC 는 테이블마다 **PK=0 레코드 1건**을 유지한다 → `SELECT COUNT(*)` 는 `queryDef operation="count"` 보다 항상 1 크다. 원시 SQL 끼리 비교하는 경우(`populationCountSql` ↔ `probe.total`)에는 기준이 같으므로 보정하지 않는다 |
| 샘플 재현성 | Oracle `FETCH FIRST` / MSSQL `TOP` 은 `ORDER BY` 없으면 비결정적 → `limitSelect` 가 기본 `ORDER BY 1` 을 붙인다. `auditSample` 은 승인 근거 자료이므로 재현성 필수 |
| 행 제한 SQL 조립 | **`dialect().limitSelect(selectList, fromClause, whereSql, n, opts)` 가 유일한 래핑 지점.** `opts = {distinct, orderBy}`. `limit(sql, n)` 은 **제거됨**(파생 테이블 이중 래핑 유발) |
| `DISTINCT` 위치 | **`selectList` 문자열에 `DISTINCT` 직접 삽입 금지.** `opts.distinct = true` 로 넘긴다. T-SQL 은 `SELECT [ALL\|DISTINCT] [TOP n] select_list` 순서라 `SELECT TOP n DISTINCT …` 는 구문 오류 ([문서](https://learn.microsoft.com/en-us/sql/t-sql/queries/select-transact-sql)) |
| 파생 테이블 `ORDER BY` | MSSQL 은 뷰·인라인 함수·**파생 테이블·서브쿼리·CTE 내부의 `ORDER BY` 를 거부**한다(TOP/OFFSET 동반 시에만 허용). 완성된 SELECT 를 감쌀 때는 `{orderBy: null}` 로 호출하고, 집합연산(`EXCEPT`/`MINUS`) 내부에도 `ORDER BY` 를 만들지 않는다 ([문서](https://learn.microsoft.com/en-us/answers/questions/1061604/the-order-by-clause-is-invalid-in-views-inline-fun)) |
| 예외 처리 | 빈 `catch {}` 로 삼키지 않는다(최소 `logWarning`). 스키마 로드 실패가 "0건"으로 위장되면 Triage 가 근거 없이 `no_column` 을 낸다 |
| 방언 지원 범위 | **ACC v7 은 PostgreSQL·Oracle·MSSQL 을 지원하지만 본 시스템은 PostgreSQL 에서만 실동 검증된다.** Oracle/MSSQL 분기는 `tools/checkDialectSql.js` 의 **문자열 정합까지만** 보증하며 실행 검증은 하지 않는다. `preflight()` 가 PG 아닌 DBMS 에서 `logWarning` + `dialectVerified:false` 를 돌려주되 **차단하지는 않는다** ([호환성 매트릭스](https://experienceleague.adobe.com/en/docs/campaign-classic/using/release-notes/compatibility-matrix)) |
| `getDBMSType` 데이터소스 | 인자 생략 시 `nms:default` 기준이다. FDA 로 외부 데이터소스를 붙이면 fragment SQL 대상 방언이 달라지므로 `dialect(dsName)` 확장이 필요하다 — **현재 미구현, `dialect()` 상단 주석으로만 표기** ([문서](https://experienceleague.adobe.com/developer/campaign-api/api/m-Application-getDBMSType.html)) |

### 6) 배포 마지막 단계 — 스모크 실행 (필수)

`tools/checkRhinoSyntax.js` 는 구문 오류만 잡고 **API 시그니처 오용은 잡지 못한다**
(문법 정상 + 존재하지 않는 메서드 호출 → 런타임 전멸). 따라서 배포 절차의 마지막 단계로
`new_ver/tools/testWooSmoke.js` 를 WF JS 액티비티에 붙여 **1회 실행한다.**

| 단계 | 검증 대상 |
|---|---|
| 1 | `testWoo.*` 14개 모듈 전역 정의 (loadLibrary 실패 즉시 검출) |
| 2 | `probe.preflight()` → `ok:true` (`sql` named right) + `dbms`/`dialectVerified` 요약 기록 |
| 3 | `sqlSelect("row,@x:string", …)` 반환 XML 구조 `logInfo` (파싱 가정 확정) |
| 4 | `describe_schema`(`TW_SMOKE_DESCRIBE_ID` = `woo:testWooAiFragment`) 속성 배열 비어있지 않음 (Schema `toDocument` 경로 회귀 검출) |
| 4b | 같은 스키마에서 `sqlColumn`(=`@sqlname`) 노출 + `name` 과 다른 컬럼 1개 이상 존재 (논리명 유출 회귀) — 실행 없음 |
| 4c | `probe_values` 에 **논리명**(`region`) 을 넣어 물리명(`sRegion`) 해석 + 실제 SQL 실행 확인 |
| 5 | `search_columns` 1건 이상 + `schemaLoadFailed` 아님 |
| 5c | **허용 namespace 전수 `list_schemas`** — `toolkit.env().allowedNamespaces` 각 항목이 실제로 조회되는지. 목록은 Triage 프롬프트에 그대로 주입되므로 여기서 깨지면 판정 근거가 흔들린다 |
| 5b | 방언별 `limitSelect` 생성 SQL 문자열 일치 (mssql/oracle/pg + 파생 테이블 래핑) — 실행 없음 |
| 6b | 존재하지 않는 id(`-1`) 조회 → **예외 아닌 `null`** (`getIfExists` 규약) |
| 6a | 큐 더미 1건 insert → `foundry.peekQueue`(=`_getQueue`) + `repo.getQueueStatus` 양쪽 조회 → 삭제 |
| 7 | **`llm.decomposeSlots("서울에 사는 고객")` 실호출** — 슬롯 1건 이상, `finish_reason="length"` 면 즉시 FAIL (반복 루프 회귀) · **(billable)** |
| 8 | **`llm.postEmbedding` 실호출** — 벡터 길이 > 0. `env llm.embedEnabled=false` 면 SKIP, 권한·미지원 오류는 FAIL · **(billable)** |
| 9 | **`foundry.dryRunSlot("서울에 사는 고객")`** — Triage+생성 경로를 큐·WF 없이 즉시 검증. `fragDoc` 존재 + `gate.pass` 면 PASS. 실패 시 shape/게이트 코드 나열 · **(billable)** |

> **스모크는 LLM 스텝(7·8·9)을 포함해 전량 실행한다. LLM 스텝을 생략한 스모크로는
> 배포를 완료로 간주하지 않는다.** 1~6 은 전부 DB/스키마 계열이라 LLM 파라미터·생성 단계
> 결함이 스모크를 통과한 뒤 **항상 마케터 입력·배치 시점에** 처음 터졌다
> (로그 #80·#83·#84·#89 동일 경로). 부득이한 경우에만 스크립트 상단
> `TW_SMOKE_SKIP_LLM = true` 로 7·8·9 만 건너뛴다(기본 `false`).
> 7·8·9 는 실제 토큰 과금이 발생하므로 요약에 `(billable)` 로 표기된다.
> 9 는 `dryRunSlot` — 큐 Write·publish 없이 생성 경로만 검증한다
> (`new_ver/workflow/testWooFoundryDryRun.js` 로 단독 실행 가능).
> 8 은 chat 과 경로가 다르다(`/api/v1/embeddings`) — urlPermission `urlRegEx` 가
> `https://openrouter\.ai/api/v1/.*` 형태여야 chat·embedding 둘 다 통과한다.

> 실패 1건 이상이면 마지막에 `logError` 가 찍힌다. **배포를 완료로 간주하지 말 것.**
> `SKIP` 은 전제 조건 미충족(4c 는 `woo:testWooSampleCustomer` 미배포 시 SKIP)이며
> PASS 로 세지 않는다. 요약에 `n skipped` 로 표기되므로 사유를 확인할 것.
> 4·4b 대상을 `woo:testWooAiFragment` 로 두는 이유: `foundry.namespaces` 가 **`woo` 하나**라
> `nms:recipient` 는 툴에서 거부되고, 코어 스키마는 **시드 여부와 무관하게 항상 배포**된다
> (샘플 테이블은 미배포 가능 → 4c 처럼 SKIP 되어야 하는 대상). `sqlname` 을 선언하지 않아도
> ACC 가 배포 시 타입 접두사로 생성하므로(`name`→`sName`) 논리·물리 불일치 컬럼이 보장되고,
> SQL 실행이 없어 비용은 0 이다. 5 의 검색 키워드도 같은 이유로 `category` 를 쓴다.
> 4c 를 샘플 테이블(100행)로 한정하는 이유: `probe_values` 는 `COUNT(DISTINCT …)` 를
> 실행하므로 대형 표준 테이블에서는 스모크 비용이 과도해진다.
> 6a 더미 레코드는 `finally` 에서 삭제되며, 삭제 실패는 `logError` 가 아니라
> `6a.queue.cleanup` **FAIL** 로 남긴다 — `logError` 는 스크립트를 즉시 끊어
> 이후 스텝과 요약 출력까지 막기 때문이다(아래 8 규약).
> 6a 가 두 경로를 모두 확인하는 이유: `_getQueue` 는 Foundry 큐 처리 경로,
> `getQueueStatus` 는 Studio 조회 경로로 **둘 다 `operation="getIfExists"`** 이기 때문이다.
> `peekQueue` 는 이 점검을 위한 읽기 전용 진입점이며 운영 로직에서는 호출하지 않는다.

### 7) LLM 요청 파라미터 규약 (코드 계약)

| 항목 | 규약 |
|---|---|
| `response_format` | **`{type:"json_object"}` 를 쓰지 않는다.** Gemini 계열은 structured output 강제 시 같은 토큰을 반복하다 `max_tokens` 를 소진한다(`completion=상한`, `reasoning=0`). JSON 강제는 시스템 프롬프트 `OUTPUT JSON ONLY` + `_parseJson` 의 `{…}` 추출이 담당한다 ([보고 사례](https://discuss.ai.google.dev/t/gemini-2-5-flash-repeats-tokens-until-max-tokens-reached-in-structured-output/107176)) |
| `tools` + `response_format` | 병용 자체가 Google API 에서 거부된다(`Function calling with a response mime type … unsupported`). 위 규약을 지키면 발생하지 않는다 |
| `reasoning` | 끄는 형식은 `{enabled:false, max_tokens:0}` **하나만** 사용(`testWoo.llm.reasoningOff()`). `enabled:false` 만으로는 Gemini 의 `thinkingBudget` 이 0 이 되지 않는다 ([OpenRouter](https://openrouter.ai/docs/use-cases/reasoning-tokens)) |
| 출력 상한 | 단계별로 분리한다 — `testWooEnv.js` `llm.pass0MaxTokens` / `pass1MaxTokens` / `triageMaxTokens` / `foundryMaxTokens`. fragment 가 쌓여 Pass1 이 절단되면 **`pass1MaxTokens` 만** 올린다 |
| 반복 억제 | `temperature` 는 `0` 유지(결정성). 반복은 `frequency_penalty` 로만 억제하며 `llm.repetitionGuardEnabled` + `llm.frequencyPenalty`(0~1, 권장 0.1) 가 0 초과일 때만 body 에 실린다 |
| 임베딩 엔드포인트 | 하드코딩 금지. `cfg.llm.endpoint` 의 `/chat/completions` 를 `/embeddings` 로 치환해 **같은 호스트를 재사용**한다. 호스트가 달라지면 urlPermission 을 추가해야 한다 |
| 진단 로그 | `finish_reason="length"` 면 `_lengthDiag` 가 `stage`/`completion`/`reasoning`/`contentChars` 와 **응답 content 앞뒤 200자**를 남긴다. **프롬프트 본문은 남기지 않는다**(고객 실데이터 유입 가능) |

> 판독 요령: `reasoning` 이 completion 의 절반 이상 → 사고 토큰 소진(`reasoningOff` 확인).
> `reasoning=0` 인데 `completion` 이 상한 → **반복 루프**(`[반복 루프 의심]` 문구가 붙는다).
> 둘 다 아니면 진짜 절단 → 해당 단계 상한을 올리거나 후보 수를 줄인다.

### 8) WF 로깅·툴 루프 규약 (코드 계약)

| 항목 | 규약 |
|---|---|
| `logError` 위치 | **WF JS 안에서 `logError` 는 스크립트 실행을 즉시 중단시키고 인스턴스를 오류 정지시킨다**([문서](https://experienceleague.adobe.com/en/docs/campaign/automation/workflows/advanced-management/javascript-scripts-and-templates)). 따라서 `logError` 뒤의 코드는 실행되지 않는다 — 상태 저장(`_updateQueue`)을 **반드시 먼저** 하고 로그를 나중에 남긴다 |
| 건별 실패 로그 | 배치의 건별 실패는 `logWarning` 으로 남긴다. `logError` 를 쓰면 첫 실패 건에서 WF 가 멈춰 **뒤 큐가 처리되지 않고** `status=processing` 으로 갇힌다 |
| 배치 중단 로그 | `logError` 는 배치 전체를 멈춰야 하는 설정 오류(`sql` right 미보유 등)에만 쓴다. 이때 WF 는 오류 정지되므로 **권한 부여 후 수동 재시작**이 필요하다 |
| 툴 루프 마지막 턴 | 마지막 턴은 `tool_choice:"none"` 으로 최종 JSON 을 강제한다. 강제 턴이 없으면 모델이 턴 전부를 툴 호출로 소진해 `tool loop exceeded` 로 끝난다 (`triage.maxTurns` 권장 6, `foundry.maxTurns` 권장 6) |
| 허용 namespace 범위 | 테스트는 **`woo` 하나**(`foundry.namespaces`). `nms` 를 열면 LLM 이 표준 스키마 50건을 훑다 턴을 소진하고 `nms:common` 같은 기술 스키마로 오답을 낸다(실측). `cus` 는 이 인스턴스에 0건이다. 넓힐 때는 **스모크 4·4b 대상 스키마와 `grainKeyCandidates` 를 함께** 되돌린다 |
| 프롬프트 힌트 실재성 | `grainKeyCandidates` 는 허용 namespace 안에 실재하는 물리 키만 넣는다. 닿을 수 없는 키를 남기면 그 키로 SQL 을 만들다 게이트에서 실패하며 턴을 소진한다 |
| 허용 namespace 노출 | Triage 프롬프트에 `toolkit.env().allowedNamespaces` 를 주입한다. 툴 설명의 `e.g. nms, cus, woo` 는 **예시일 뿐 허용 집합이 아니다** — 알려주지 않으면 모델이 미허용 ns 를 찍어보며 턴을 낭비한다 |
| 툴 실패 가시성 | `invoke` 실패는 `ok=false` 만 찍지 않고 **사유까지** `logWarning` 한다. `search_columns` 는 미허용·조회실패 namespace 를 `skippedNamespaces` + `partialScan:true` 로 돌려준다(조용한 스킵은 "조사했으나 0건" 위장 → 근거 없는 `no_column`) |
| 값 vs 컬럼 검색 | `search_columns` 는 컬럼 **이름·라벨**만 매칭한다. `서울` 같은 **데이터 값**으로 검색하면 0건이 나오므로 후보 컬럼을 먼저 찾고 `probe_values` 로 값을 확인하도록 프롬프트에 명시한다 |
| Foundry 출력 계약 | `_foundrySystemPrompt` 에 fragment JSON 스키마(`name/keyColumn/sqlText/…`)와 게이트 규칙을 **명시**한다. 스키마 없는 "Output JSON" 만으로는 `fragment JSON missing` 이 난다(로그 #89·리포트 08) |
| 생성 루프 종료 | `requireJson:true` 이면 툴 없는 평문 턴에서 return 하지 않고 nudge 후 계속. `tool_calls` 유무는 `finish_reason` 이 아니라 배열로 판정 |
| 형태 실패 재시도 | JSON 파싱·필수 필드 누락도 게이트와 동급으로 되먹임(`gateRetries`). attempt 루프 안에서 throw 금지 |
| 생성 경로 계측 | `foundry.dryRunSlot(slotText)` — 큐·WF 없이 Triage+생성 1회. 스모크 9번·`testWooFoundryDryRun.js` |
| 툴 단계 예산 | `setPhaseBudget("triage"\|"generate")` + `triageCallBudget`/`generateCallBudget`. 요청 전체 `totalCallBudget` 기본 40 |

> 실패 계보: 큐 27581 은 Triage 가 4턴(하드코딩)을 값-검색으로 소진 → `tool loop exceeded`
> → `catch` 에서 `logError` 가 먼저 실행되어 `_updateQueue` 가 유실 → `SCR-160012` 로 WF 정지 +
> 큐가 `processing` 에 갇혀 **`staleProcessingMinutes`(30분) 동안 배치 전체가 스킵**됐다.

### 섹션 6 완료 체크

- [ ] WF 실행 오퍼레이터에 `sql` named right 부여 → `preflight()` `ok:true`
- [ ] `WKF_testWooFoundry` 동시 실행 금지 설정
- [ ] 스키마 4종 재등록 + Update database structure (`dedup_*` 3필드 확인)
- [ ] JS 라이브러리 전량 재배포 (`Env` → `Config` → … → `Foundry`)
- [ ] WF 2회 연속 수동 Run → 두 번째가 `skipped:true`
- [ ] **`testWooSmoke.js` 실행 → 1~9 전 항목 PASS** (`logError` 없음 · 7·8·9 생략 금지)
- [ ] 스모크 `5c` 의 `allowedNamespaces=` 가 **`woo`** 이고 조회 건수가 0 이 아닌지
      (ACC 에서 손으로 고치지 말 것 — 재배포 때 `testWooEnv.js` 값으로 덮인다)
- [ ] WF 실패 후 큐 행에 `status`/`err_id`/`last_error` 가 **남아 있는지**
      (비어 있으면 `logError` 가 저장보다 앞선 회귀)
- [ ] Studio 에서 `서울에 사는 고객` 입력 → `max_tokens` 절단 없이 Pass0 통과
- [ ] `describe_schema` 결과에 속성이 실제로 채워지는지 육안 확인
- [ ] 5b 방언 3종 생성 SQL 육안 확인 (MSSQL 이 `DISTINCT TOP n` 순서인지)
- [ ] 스모크 요약의 `dbms=` 가 PostgreSQL 이고 `dialect verified` 인지 확인
      (PG 가 아니면 `preflight()` 경고가 남으며, 방언은 문자열 정합만 보증된 상태)
- [ ] 배포 전 로컬: `node tools/checkRhinoSyntax.js` + `node tools/checkDialectSql.js` 통과

---

## 섹션 7 — WF 캔버스 ↔ Studio ↔ 커스텀 액티비티 (다음 적용)

> **지금 위치:** 섹션 1~3 완료 · 섹션 4 Studio(`/woo/`)·Register 동작 · 섹션 5 샘플 시드·NL 라운드트립 진행 중 · 섹션 6 Foundry·스모크 진행 중.  
> **아직 안 된 것:** 캔버스에서 Studio 열기 · WF별 SQL 사이드 목록 · 커스텀 액티비티 `ai_sql-id` 런타임 E2E.  
> 그게 **이 섹션(7)** 의 범위다. “섹션 5까지 끝”이 아니라 **4는 UI API, 5~6은 데이터·Foundry, 7이 WF 제품 진입점**이다.

### 1) 최종 목표 (제품)

| 요구 | 계약 |
|---|---|
| 콘솔 로그인 계정 | Studio는 기존과 동일 — Cookie + `logonWithToken`. 별도 SSO 없음 |
| 현재 연 WF 연동 | URL `?workflowName=<internalName>` (`@id` 금지 — 패키지 이동 시 변경) |
| Register | `woo:testWooAiSql`에 저장 + `workflow_name` 기록 → `{ ai_sql_id }` |
| 커스텀 액티비티 | 속성 `ai_sql-id`로 이력 조회·실행. **WF XML에 SQL 주입 금지** (`WorkflowIo` 폐기 유지) |
| 액티비티 없음 | Studio가 안내 + 설치/계약 문서 링크 (자동 생성하지 않음) |
| 여러 SQL | 같은 `workflow_name` 이력을 Studio **오른쪽 패널**에 목록. 불러오기·재등록 |

### 2) 진입점 — 확장 폼 안에 Studio 임베드 (주 UX)

마케터 주 경로: **Campaign > Targeting and workflows** → Properties 옆 **AI Studio**.  
Adobe Overview WebApp/대시보드와 같이 **콘솔 WebView(Edge Chromium)** 안에 HTML을 띄운다  
([Web applications](https://experienceleague.adobe.com/en/docs/campaign-classic/using/designing-content/web-applications/designing-a-web-application),  
[Edge Chromium](https://experienceleague.adobe.com/en/docs/campaign-classic/using/technotes/technote-migration/edge-chromium)).

| 수단 | 역할 |
|---|---|
| `entity-schema="xtk:workflow"` | 확장 폼 = 현재 캔버스 WF 컨텍스트 |
| `woo:testWooExtendWorkflow:lib/aiStudio` | CA 상태 + Studio iframe + Apply |
| `BuildStudioUrl` | Option 호스트 + `embed=1` URL + iframe HTML |
| `InspectAiTarget` | 폼 `activities` DOM 에서 `customActivity` 유무 (DB 스냅샷 아님) |
| `BindAiSqlId` | `ai-sql-id` 만 기록 → WF **Save** 로 확정 (SQL 주입 금지) |

| 파일 | 역할 |
|---|---|
| `new_ver/input_form/testWooExtendWorkflow.xml` | 임베드 셸 |
| `new_ver/js/testWooWorkflowUi.js` · `schema/testWooAiWorkflowUi.xml` | SOAP |
| `new_ver/workflow/testWooXtkWorkflowRedirectPatch.xml` | Properties 옆 버튼 스니펫 |
| `new_ver/jssp/testWooAiStudio.jssp` | `?embed=1` 크롬리스 레이아웃 |

```
Targeting → AI Studio 다이얼로그
→ iframe Studio (NL→Register) + CA 상태
→ ai_sql_id Apply to canvas → WF Save
iframe 실패 시: 같은 창의 Studio URL 을 외부 Chrome/Edge 에 붙여넣기 (폴백)
호스트: Option testWooAiStudioBaseUrl (끝 / 없음)
```

### 3) Studio 화면 확장 (섹션 7에서 추가)

| 영역 | 내용 |
|---|---|
| 왼쪽 (기존) | NL · 슬롯 · 자동 검증 · SQL · Register |
| **오른쪽 (신규)** | `workflowName`으로 필터한 `woo:testWooAiSql` 목록 (title · 일자 · status · ai_sql_id) |
| 목록 동작 | 행 클릭 → SQL/plan 불러오기 · “이 SQL을 액티비티에 연결” 안내(`ai_sql_id` 복사 또는 설정 도우미) |
| API | `testWooAiValidate.jssp` catalog 확장 **또는** `testWooAiSqlList.jssp?workflowName=` (권한 Register/Generate) |

`workflowName` 없으면: 목록 숨김 + “WF에서 AI 버튼으로 열어 주세요” 배너 (지금처럼 수동 URL도 허용).

### 4) 커스텀 액티비티 계약 (E2E)

| 항목 | 내용 |
|---|---|
| 입력 | 액티비티 속성 **`ai_sql-id`** (long) = Register가 준 id |
| 런타임 | `woo:testWooAiSql`에서 `sql_query` 로드 → work table → transition |
| 금지 | `<script>`에 SQL 덮어쓰기 · WF XML Write |
| 샘플 참고 | `new_ver/workflow/testWooSampleCustomActivityContract.xml` — **구 “script inject” 주석은 폐기 예정**. 계약은 `ai_sql-id`만 |
| 없을 때 | 확장 폼 Status: customActivity 없음 안내. **자동 생성 안 함** |
| 캔버스 반영 | Register → `ai_sql_id` Apply → `activities` 의 `ai-sql-id` 갱신 → 운영자 WF Save |

### 5) 구현 순서 (섹션 7 체크리스트)

| 단계 | 내용 | 코드 상태 |
|---|---|---|
| **[7a′]** | Properties 옆 버튼 + 확장 폼 | ✅ |
| **[7b]** | Studio 우측 SQL 목록 | ✅ |
| **[7c]** | Register · `ai_sql_id` | ✅ |
| **[7e′]** | 폼 임베드 Studio + Inspect/Bind (주 UX) | ✅ 저장소 · **콘솔 재배포** |
| **[7d]** | 커스텀 액티비티 `ai_sql-id` 런타임 E2E | ⬜ 다음 |

### 5-1) 7e′ 콘솔 재배포 순서

| # | 작업 | 비고 |
|---|---|---|
| 1 | Option `testWooAiStudioBaseUrl` | **필수** · 끝 `/` 없음 |
| 2 | 스키마 `woo:testWooAiWorkflowUi` | Build · Inspect · Bind |
| 3 | JS Code `woo:testWooWorkflowUi.js` | 최신 파일 전체 교체 |
| 4 | Input form `woo:testWooExtendWorkflow` | iframe + Apply |
| 5 | JSSP Studio(+Js) | `embed=1` |
| 6 | `xtk:workflow` AI 버튼 | 이미 있으면 스킵 |
| 7 | Targeting PoC | NL → Register → Apply → WF Save |

**#4~5 재배포**

1. 스키마 `woo:testWooAiWorkflowUi` ← `new_ver/schema/testWooAiWorkflowUi.xml`  
2. JS Code `woo:testWooWorkflowUi.js` ← `new_ver/js/testWooWorkflowUi.js`  
3. Input form `woo:testWooExtendWorkflow` ← `new_ver/input_form/testWooExtendWorkflow.xml`  
4. JSSP `testWooAiStudio` / `testWooAiStudioJs` 재배포  

### 6) 섹션 7 완료 체크

- [ ] Targeting 툴바 **AI Studio**  
- [ ] 다이얼로그에 Studio iframe(또는 URL 폴백) + CA Status  
- [ ] customActivity 없을 때 안내 / 있으면 Apply 가능  
- [ ] Register → `ai_sql_id` Apply → WF Save 후 액티비티에 id 반영  
- [ ] WF XML에 SQL 문자열 없음  
- [ ] 커스텀 액티비티 런타임 추출 1회 (**7d**)  

### 7) 하지 말 것

- 원본 `xtk:workflow` 본문 전체 교체  
- Register/Bind 가 SQL/`<script>` 를 WF에 쓰게 되돌리기  
- CA 자동 생성  
- 호스트를 인풋폼에 하드코딩 (Option 사용)

---

## 적용 섹션 진행

| 섹션 | 내용 | 상태 |
|---|---|---|
| 1. 기반 세팅 | 권한 + LLM 옵션 3 | ✅ |
| 2. 데이터 모델 | fragment·이력 스키마·폼 | ✅ |
| 3. JS 라이브러리 | 서버 JS · CNF · WorkflowIo 폐기 | ✅ |
| **4. JSSP + Studio** | `/woo/` · 인증 · Generate/Validate/Register | ✅ 실사용 (게이트 UI #97) |
| **5. 샘플·NL E2E** | 시드 · fragment · Studio NL→SQL | 🔄 진행 (T1~T3) |
| **6. Foundry** | sql right · 스모크 · 큐 배치 | 🔄 진행 (스모크 15/15) |
| **7. WF 연동** | 임베드 Studio · Inspect/Bind · SQL 목록 · CA 런타임 | 🔄 7e′ 코드 완료 · 콘솔 재배포·**7d** 남음 |
| (병행 설계) | Fragment 생애주기 · 승인→라벨 | 📄 `10_Fragment_생애주기_설계.md` — 런타임은 T3 후 |

**다음에 할 일 (권장 순서)**

1. 콘솔: 스키마/JS/확장 폼/JSSP 재배포 (#4~6)  
2. PoC: Targeting AI Studio → iframe NL → Register → Apply → WF Save  
3. **7d** 커스텀 액티비티 런타임 E2E  
4. 생애주기 1단계(auto-active)는 별도  

---

_가이드 v1.7.5 · 섹션7 주 UX — 확장 폼 임베드 Studio + Inspect/Bind `ai_sql-id`._

_가이드 v1.7.4 · 섹션7 진입점 — 캠페인 Targeting용 Properties 옆 flatSubFormButton._

_가이드 v1.7.3 · 섹션7 진입점 — `entity-schema` 확장 폼 + 원본에 `ref` 한 줄._

_가이드 v1.7.2 · `woo:testWooExtendWorkflow` + ref 한 줄. AI = iconbox 탭._

_가이드 v1.7.1 · SOAP `BuildStudioUrl` · listSql/getSql · Studio 우측 패널.
커스텀 액티비티 계약 = `ai_sql-id` only._

_가이드 v1.7.0 · 섹션 7 WF 캔버스↔Studio↔커스텀 액티비티 신설._

_가이드 v1.6.4 · Foundry 생성 단계 F-0~F-5 (`dryRunSlot` · 출력 계약 · requireJson ·
형태 재시도 · 단계 예산) + 스모크 **9(foundry.generate)** · 섹션6-8 규약 보강._

_가이드 v1.6.3 · `foundry.namespaces` 를 **`woo` 단독**으로 확정(테스트 범위 = 샘플 테이블,
nms 개방 시 LLM 이 표준 스키마를 훑다 오답). 연동 조정: 스모크 4·4b 대상 →
`woo:testWooAiFragment`, 5 키워드 → `category`, `grainKeyCandidates` → `sCustomer_id`._

_가이드 v1.6.2 · 섹션6 에 **WF 로깅·툴 루프 규약** 신설(`logError` 는 스크립트를 중단시키므로
상태 저장을 먼저 · 건별 실패는 `logWarning` · 마지막 턴 `tool_choice:"none"` 강제 ·
허용 namespace 프롬프트 주입 · 툴 실패 사유 로깅) + 스모크 **5c(허용 namespace 전수 조회)** 추가._

_가이드 v1.6.1 · 섹션6 에 **LLM 요청 파라미터 규약** 신설(`json_object` 사용 금지 ·
`reasoningOff` 단일 형식 · 단계별 출력 상한 · `frequency_penalty` 반복 억제 ·
임베딩 엔드포인트 호스트 재사용 · length 진단 판독 요령) + 스모크 **7(llm.pass0)·8(llm.embedding)**
추가 및 "LLM 스텝 생략 시 배포 완료 불인정" 명시._

_가이드 v1.6.0 · 섹션2 에 **논리명 vs 물리명 규칙**(`sqlname` 접두사 대응표 ·
`sql_text`/`key_column` 물리명 필수 · PK=0 기술 레코드) 신설, 샘플 fragment SQL 을
실측 물리명으로 교정. 섹션6 에 물리 컬럼명/테이블명/기술 레코드 규약 반영 +
스모크 **4b(sqlColumn 노출)·4c(논리명→물리명 해석 실행)** 추가, SKIP 상태 도입.
기존: getIfExists 반환형 · getSchema toDocument ·
행 제한 SQL 조립(limitSelect 단일 래핑 · DISTINCT 위치 · 파생 테이블 ORDER BY 금지) ·
방언 지원 범위(PostgreSQL 만 실동 검증, 나머지는 문자열 정합만) 규약과
배포 마지막 단계 스모크(`testWooSmoke.js`, 4b/4c/5b/6b/6a 포함)._
