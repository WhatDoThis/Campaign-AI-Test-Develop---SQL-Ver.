---
description: Campaign AI Studio project context — read docs before changing new_ver
alwaysApply: true
---

# Campaign AI Studio (Test Woo)

LG U+ **Adobe Campaign Classic v7** — NL audience extraction via fragment library.

## Source of truth (read first)

1. `docs/main/PRD.md` — design principles (AI never writes SQL; trust pipeline)
2. `docs/report/01_개발가이드.md` — naming, deployment sections, ACC constraints
3. `.cursor/skills/campaign-ai-studio/SKILL.md` — agent workflow + file map

## Naming

- Namespace: `woo` | Files/schemas: `testWoo*` camel | Global JS: `testWoo.*`
- URL prefix: `/woo/` | Options/rights: `testWooAi*`
- Never use `uplus` in new code

## Scope guardrails

- Implement only in `new_ver/` unless user asks otherwise
- DEFERRED unless explicit: codemap, request_queue, funnel COUNT, WF XML inject
- Modify `docs/main/*` only when user requests doc updates
- After file changes: update `docs/log/log.md`

## Code standards

- Module header: `.cursor/rules/module-header-docstring.mdc` (역할 / Main Functions / Dependencies · HUMAN READ)
- ACC server JS: Rhino-safe — see `.cursor/skills/campaign-ai-studio/acc-rhino-constraints.md`
- **No domain hardcoding** — see `.cursor/rules/no-domain-hardcoding.mdc` (운영: 스키마×컬럼×조합 폭발; JS는 엔진만)
- Minimize diff; follow existing `testWoo*.js` patterns
- New/edited code files: refresh header in the same turn
- **Bug fix workflow**: `.cursor/rules/fix-downstream-review.mdc` — 단편 패치 후 완료 선언 금지; pipeline hop 검수 후 답변
