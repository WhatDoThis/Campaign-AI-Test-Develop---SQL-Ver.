# xtk:workflow form backup

Before applying `../testWooXtkWorkflowButtonPatch.xml`:

1. In Console: **Administration > Configuration > Input forms** → open `xtk:workflow`
2. Export / copy the full XML here as `xtk_workflow_ORIGINAL.xml`
3. Keep a dated copy if you re-patch after an Adobe upgrade merge conflict

Do not commit production hostnames or secrets in the backup file.
